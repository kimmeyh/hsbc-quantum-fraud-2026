# Changelog

Policy (disposition item 8, adapted from spamfilter-multi ADR-0025): updated in the SAME commit as the change it describes, under `## [Unreleased]`, grouped by date, newest first. Format: `- **type**: Description (Issue #N / PR #N)` with type in feat | fix | docs | test | chore | process. Entries move under a version heading when develop merges to main at a milestone tag.

## [Unreleased]

### 2026-09-23

- **docs**: **Phase 8 sweep for Sprint 18.** The master plan's "Last Completed Sprint" still described Sprint 14 through four later sprints, and its sprint table stopped at 14; both rolled. F91 pruned. F90's balance figures corrected from 1,961 to 1,681 (the 280 probe seconds), and F5's stale "grant not granted" HOLD reason corrected. SPRINT_18_SUMMARY.md was written after round 1 and never updated for round 2 or F91: it reported 16 metered seconds and a falsified line, now corrected with a dated note. CHECKLIST-Phase2-pre.md had F73 and F64 shipped and unticked
- **process**: **Two CI checkpoints, neither blocking** (3.3.2, about five minutes after the draft PR exists; 7.0, on the final HEAD without waiting). A red check is handed to a background agent and the sprint continues; the close-out hook blocks the completion claim if CI is not green by 7.7. A four-phase version written an hour earlier was cut back as too much process: the Sprint 18 failure was that nobody looked ONCE. IMP-2 dropped by the team lead rather than carried (PR #139)
- **fix**: **CI was red on every commit of Sprint 18, and the cause was not the sprint's work.** `test_phase3_artifacts.py` sent the Stop hook a payload with no `branch_override`, so on the detached HEAD that `actions/checkout` leaves the hook read an empty branch name and returned ALLOW before any check. Three tests could fail only on Linux and could not fail locally on any run. Reproduced in a detached worktree before the fix (PR #139)
- **fix**: **PR #139 review, three reviews, every finding reproduced before it was acted on.** The Phase 2 ask's 7,500 was a deliberate round-up of 9,000 - 1,681 = 7,319 that the document presented as the result of a subtraction; it now names the rounding. Two compounding buffers were labeled as parallel percentages. A claimed test of the sizing rejection message tested an inline copy of nothing. The metered runner now distinguishes a call that failed before submission from one that may have billed, and an unapproved ~181 s design is reachable only by name. The F64 decomposition guard passed while the interaction read +0.0011 instead of +0.0001 (PR #139)
- **process**: Sprint 18 retrospective. IMP-1 (actuals recorded at task completion in `sprint_status.json`), IMP-3 (a metered runner is idempotent before its first approval, added to Criterion H), IMP-4 (probe designs are configuration in `experiments/probe_designs.json`, selected with `--only`) and IMP-5 (`scripts/outward_readability.py` flags repository-only tokens in outward documents) implemented. IMP-2 held, then dropped
- **docs**: **The Phase 2 ask is reconciled against the original sponsorship request** (2,000 s preregistered plus 20,000-40,000 s TBD). The proxy absorbs four of six experiments, the null narrowed the grid, and measured fit costs came in below the estimate that sized the original ask. A guard pins the comparison
- **feat**: **F91: the fourth ladder corner turns F64's bound into an attribution.** k=13 at order 3, 377 variables, ten seeds, zero metered seconds. Order effect +0.0245, CI [+0.0187, +0.0302]; feature-count effect +0.0001, CI spanning zero; interaction about +0.0001. B2's +0.0256 comes from three-feature learners, not from more features. The QCi hardware plan now opens with the Phase 2 ask: 9,000 seconds, per-block table at measured rates
- **feat**: **Integer probe round 2: cost tracks VARIABLES, not the level budget.** Three more calls, 264 metered seconds. A controlled pair settled it: 60 variables at 840 levels cost 71 s, 150 variables at 600 levels cost 165 s. Round 1's 0.0625 s-per-level line (the entry below) was falsified by 1.6x and 4.1x at the two far points. The block stopped itself after two of four approved calls when the measured points put the remainder near 181 s each against ~55 s quoted; the team lead approved one more. Allocation 1,961 to 1,681
- **feat**: **F87: the Dirac-3 integer path has a measured cost.** (The level-budget reading in this entry was superseded the same day by round 2, above.) 4 metered seconds at 32 levels, 8 at 96 -- the first integer-solver data this project has, since Phase 1 ran only the convex continuous relaxation. The QCi hardware plan's "we are not quoting a cost for that block" paragraph is replaced by the measured table. The binding quantity is the LEVEL BUDGET, `sum(upper_bound + 1)`, not the variable count: `num_levels` is computed by `solve()`, not passed to it, and the card assumed otherwise
- **fix**: **Three calls were made against a two-call approval**, 16 metered seconds instead of 12. The runner had no memory of what it had already submitted, so a second invocation with a higher `--max-calls` re-ran the first probe from the top. It now reads its own ledger, skips completed labels, and refuses to submit if the ledger is unreadable. No result is affected; the duplicate returned the same 4 seconds
- **feat**: **F64: the missing middle of the ladder moves nothing.** k=17 at order 2 (153 variables) minus k=13 at order 2 (91 variables), ten paired seeds: **+0.00007, CI [-0.0024, +0.0026]**, spanning zero and an order of magnitude tighter than the MDE. B2's +0.0256 is NOT explained by feature count. Reported as a BOUND rather than an attribution, per the Sprint 13 withdrawal: no k=13 order-3 cell exists, so the interaction term is unidentified
- **feat**: **F89: an injection that cannot silently prove nothing.** `replace()` is unbounded and the survivor count is asserted to be zero; a missing target, an unexpected count, or a no-op mutation is an error rather than a quiet success. `assert_can_fail` checks three legs, because each alone is satisfiable by a vacuous test. It caught two vacuous guards on its FIRST real use
- **feat**: **F88: `verify_claim.py` answers "who has already said something about this figure?"** Tested against both Sprint 17 false findings verbatim: "1,141" surfaces SPRINT_12_SUMMARY.md, where a reviewer made the same error; "28.0" names A32, which corrected A31 on the day of submission, and surfaces the artifact the finding claimed recorded neither figure
- **fix**: `test_the_168_existing_rows_are_not_retrofitted` asserted that NO row carries an environment field. Right when written, wrong the moment A33 did its job. Pinned to the boundary rather than deleted -- a guard that fails on correct behavior gets deleted instead of fixed
- **docs**: **F2b deferred to F90 at schedule 3.** The first Sprint 18 draft ran B4 at schedule 2 on an INVERTED reading of A31: the resolution limit predicts forced sparsity as a MECHANISM, not a bad outcome, and B2 at 833 variables with weight cosine 0.83 produced the campaign's only positive result at scale. Schedule 2 is a configuration already shown to lose 8 of 8 overall. F90 carries B4 at about 1,236 s, 63% of the remaining balance, as its own allocation decision

### 2026-09-22

- **fix**: **PR #122 review: nine findings, and three shared one signature -- a probe that could not run, reported as a probe that found nothing.** `verify_closeout_complete.py` read `develop..HEAD`, which exits 128 with EMPTY stdout when develop is not a local ref, so all three Phase 3 checks were SKIPPED; CI uses `actions/checkout@v4`, which does not fetch develop, so the guard added that same sprint never fired where merges are gated. `render_all.py`'s `is_tracked()` returned `returncode == 0`, so any git failure read as "not a submitted artifact" and the refusal never fired -- the script would rewrite the three filed PDFs and print "Rendered 3 document(s)." `store.environment()` recorded a NUMPY VERSION in a field named `blas`, because `np.__config__.get_info` was removed in numpy 1.26, the pinned version, so the fallback was the live path everywhere
- **fix**: **An arithmetic error in the letter going to QCi** (Copilot). `QCI_EQC_MODELS_FEEDBACK.md` said 163 free-tier plus 1,039 allocation seconds partition the 1,141 campaign total; that sums to 1,202. Only 978 drew on the allocation -- the other 61 grant seconds went to the withdrawn B3 run, which is excluded from the campaign total. In a document whose credibility rests on every figure tracing, this was the most consequential finding of the review
- **fix**: **Three different suite counts for one sprint** (Copilot), across the summary, the retrospective and this file, none matching the suite. CLAUDE.md forbids restating suite counts; they were restated five times in the PR that cites the rule. All removed
- **fix**: `block_heredoc_escape_loss.py` was wrong in BOTH directions: it missed three file-writing forms (a redirect after the opener, a script generating a file through a redirect, and `tee` with no redirect at all), and it over-blocked six escape sequences that bash does not consume -- measured on Linux bash rather than assumed. A guard that blocks correct work gets bypassed
- **test**: `test_skip_conditions.py` had no guard-the-guard: blinding the scanner left all three tests green. A file written to detect tests that pass without running, passing without running. Two `render_all.py` guarantees asserted only as source substrings are now behavioral -- a missing source must exit 1, and `--qci-package` must leave every submitted PDF byte-identical
- **fix**: **The Phase 3 artifacts were never created for Sprint 17.** No draft PR, no task issues, `plan_approved: false` -- the sprint ran nine tasks, Manual Validation and a retrospective without them, and I told the team lead the PR was "your call", which moved my own omission onto him. Opening the draft PR is Claude's job at Phase 3; only the MERGE is his. The close-out guard missed it because it tested `plan_approved is True and pr is None`, reading one stale field to decide whether to distrust another. Recovered: draft PR #122, issues #123-#131
- **process**: Sprint 17 retrospective. Five improvements implemented (estimates derived from cards rather than asserted in the plan; no override flag in a guard's own commit; the amendment token consumed by the hook; skip conditions audited mechanically; QCi document wording requirements enforced) and two carded as F88 and F89

### 2026-09-20

- **feat**: **A deterministic status footer** (`scripts/status_footer.py`), replacing a hand-assembled line that once reported a time an hour stale. Reads the live clock every call and the sprint and phase from `.claude/sprint_status.json`. The existing PowerShell script printed "Sprint 17" with NO PHASE against this repository, because it expects spamfilter-multi's prose status convention and this repo writes snake_case slugs -- a silently dropped field, which is the same defect class as the stale timestamp
- **fix**: `test_requirements_complete.py` reported `status_footer` as an undeclared THIRD-PARTY package, because `_local_modules()` scanned only `experiments/src/` and not `scripts/`. Following its advice would have put a non-existent distribution in `requirements.txt` and broken the next fresh clone

### 2026-09-19

- **feat**: **F86: the QCi post-submission package is assembled**, seven files, for the team lead to send. Three submitted PDFs at their filed bytes (hash-verified), three refreshed documents and a plain-text memo. It asks QCi for nothing except their thoughts: the 30,000-second request was the previous letter, and re-asking inside a thank-you would undercut both
- **docs**: **The QPU figures reconcile exactly, and the obvious subtraction is wrong.** 3,000 - 1,141 = 1,859 is wrong in both directions at once: the campaign total includes 163 pre-grant free-tier seconds and excludes the 61 seconds of the withdrawn first B3 run. Drawn is 1,039, remaining is **1,961**, endpoint-confirmed in Sprint 12. A reviewer made the same subtraction then; the Sprint 17 plan's own pre-flight made a version of it again. Recorded at `docs/QPU_RECONCILIATION.md` so the next reader does not re-derive it
- **feat**: **F82: an unquoted heredoc that would eat a backslash escape is now BLOCKED** (`block_heredoc_escape_loss.py`). Nine occurrences in Sprint 16, including twice while writing the retrospective documenting the class, then twice more in Sprint 17 five minutes apart. A rule has existed since Sprint 16 and did not work, because it depends on remembering at the moment of writing. Both BLOCK cases are the real Sprint 17 commands
- **fix**: **`render_all.py` rebuilt the three SUBMITTED PDFs on any invocation**, and did so twice during this sprint -- the second time because the first fix added an `--allow-overwrite-submitted` flag and the next command passed it to get an unrelated test running. The flag is gone and there is no override; `--qci-package` now renders the package ONLY rather than CORE plus the package. Filed bytes restored from git both times and verified
- **fix**: `render_all.py` printed `SKIP (missing source)` and exited 0, so a renamed document would drop out of the package while the run reported success. Same class as the confidentiality scanner's missing-file bypass fixed in Sprint 16
- **feat**: **F83: measured on both platforms.** Zero failures on either, with identical pass and skip counts -- the counts themselves are not restated here, per the standing rule. Appendix C's "Python 3.12, Linux" is CORRECT and enforced in code: `run_hardware.py` and `tune_proxy.py` hard-exit on any non-POSIX platform because the full-pair pool build needs `fork`. No submitted document needed correcting (`docs/VENV_PARITY.md`)
- **fix**: **The cross-repository guard had never run in CI.** `test_cross_repo_write_guard.py` skipped 15 of its 17 cases on non-Windows with "powershell not on PATH", but the hook it tests is a Python file invoked through `sys.executable`. CI runs ubuntu-latest, so the guard enforcing the team lead's boundary -- added after a session wrote into EvidenceBasedDB -- passed by being skipped. Proven by injection to fire on Linux now
- **feat**: **A33: every new results row records its execution environment** (os, os_release, python, threads, and blas when numpy is loaded), stamped in `store.append_row`, the single write path. The justification is PROVENANCE, not a measured divergence: Task H found none, but none of the 168 existing rows records which machine produced it and the lock file pins no platform. The field earned itself immediately by reporting numpy 2.5.3 in the Linux environment against the locked 1.26.4. The 168 existing rows are NOT retrofitted and a test asserts they stay that way
- **fix**: `test_submission_artifacts.py` required every document to track the CURRENT amendment count. Right while the submission was live, wrong after it: the submitted documents are frozen at the filed count of 32, and editing one to match a later amendment would falsify the record. Surfaced by A33 being the first amendment written after filing
- **docs**: `HARDWARE_PLAN_PHASE_2.md` is new. The package previously rendered `HARDWARE_REQUEST_B1_G0b.md` under the name "Hardware Plan for Phase 2", but that document is the Phase 1 request for blocks B1 and G0b, executed 2026-09-03. Sending it would describe finished work as the forward plan
- **docs**: `QCI_EQC_MODELS_FEEDBACK.md` refreshed from 37 fits / 163 seconds (the free-tier period) to the full 61 / 1,141, and its two open questions now report what the campaign answered -- including the 200:1 resolution finding, which explains the 833-variable arm's 0.83 weight cosine and forced the withdrawal of a claim of ours that was backwards (A31)


- **docs**: **The three submitted PDFs are now PUBLISHED** at `docs/paper/out/`, and the committed bytes are the ones filed on 2026-09-12. Appendix C stakes the submission's reproducibility claim on a PUBLIC repository, and a reader could regenerate the PDFs but never see the artifacts actually filed -- the portal copies are invisible outside HSBC, so the repository was the only possible public record and did not carry it
- **fix**: The filed bytes had been lost. Sprint 16 Task G re-rendered all three in place to prove the converted renderer reproduced them, which it did (identical text, page counts and sizes), but a PDF carries a per-build `/ID` trailer so the bytes changed. Recovered from a 2026-09-16 Acronis backup and verified against the hashes in PACKAGE.md before commit. `test_published_artifacts.py` now fails on any rebuild of a tracked PDF
- **fix**: Three documents claimed the PDFs were "not tracked in git" and named the retired `render-all.ps1`: `PACKAGE.md`, `SUBMISSION_RECEIPT.md` and `README.md`. All corrected together, since one search found all three
- **chore**: **The QCi correspondence moved to `docs/qci_package/` and is gitignored.** Private commercial negotiation, not part of the HSBC submission. The move would have PUBLISHED it: under `docs/paper/out/` the parent rule was doing all the work, and nothing under `docs/` replaced it. The rule was written and verified before the files moved
- **chore**: All 13 PowerShell scripts moved out of the repository to `OldPowerShellScripts` (Sprint 16 IMP-5), with a README mapping each to its Python successor and warning that `confidentiality-scan.ps1` holds an unfixed single-line-file bug. The parity tests now skip their PowerShell half rather than failing
- **docs**: CLAUDE.md gained three NOT-do entries from the Sprint 16 retrospective: never re-render a submitted artifact to prove a tool works; write an ignore rule at the DESTINATION before moving a protected file; never trust an injection that has not asserted its mutation landed
- **feat**: F82 carded at Priority 4 -- catch the escape-eaten class at write time. Seven occurrences in one sprint makes it the repository's most frequent recurring defect, and the two existing hooks demonstrably do not cover the shape that keeps biting

### 2026-09-17

- **feat**: **F79: a sprint can no longer merge without its retrospective.** Sprint 15 merged to develop AND to main with none, past an exit gate. The three-doc test deliberately exempts the sprint in flight, and nothing revoked that exemption; the exemption also followed a mutable status field, so it protected the wrong sprint. Completion is now judged on evidence (a later sprint has a plan), and the close-out hook demands the retrospective independently. Its old check asked for the summary only, and only when the retrospective already existed, so a missing retrospective triggered nothing at all (Issues #110, #111)
- **feat**: **F78: all nine hooks and four scripts converted from PowerShell to Python.** CI runs `ubuntu-latest`, so every guard in this repository had been Windows-only protection and none had ever executed where CI runs. 143 parity cases run against BOTH implementations and require agreement; CI now proves a guard FIRES on Linux rather than merely loading (Issues #112-#118)
- **fix**: **The confidentiality scan reported clean on single-line files.** `Get-Content` returns a String rather than an array for a one-line file, so `$lines[0]` yielded the first CHARACTER and every rule matched nothing. A file containing an employer name or an API token scanned as "0 HIGH, 0 REVIEW". The submission was not affected (its documents are 104, 102 and 216 lines), and the PowerShell is not being fixed because it is retired; the Python cannot reproduce it and carries regression cases (Issue #116)
- **feat**: `sprint_auto_advance.py` ported from spamfilter-multi, where it had been requested and never transferred. Its four commitment patterns matched NONE of this repository's three real turn-ending failures, so a participle-opening pattern was added for the shape this repository actually produces (PR #120)
- **docs**: `docs/ENVIRONMENT.md` is the single place the venv interpreter is recorded, for Windows and Linux. It was spelled out in 21 places across 16 files and is not discoverable from the environment; a bare `python` is usually the wrong one and fails like a broken checkout (Issue #117)
- **test**: `test_interpreter_paths.py`, `test_hook_parity.py`, `test_hooklib.py`, `test_confidentiality_scan.py`, `test_render_scripts.py`, `test_premature_turn_end.py`. Every one verified by injection rather than by a green run

### 2026-09-16

- **docs**: **US English enforced across tracked markdown**, 90 replacements in 35 files, with `scripts/us_english_fix.py` committed and `test_us_english.py` guarding it. The submitted documents and the FROZEN preregistration are EXEMPT and the guard asserts that exclusion holds, so a later sweep cannot reach them (team lead, 2026-09-16)
- **docs**: **What CVQBoost actually is**, researched against primary sources and recorded in `docs/research/cvqboost-name-and-lineage.md`. QBoost encodes weak-learner weights as low-bit-depth binary expansions solved as a QUBO; CVQBoost uses continuous positive weights summing to 1. Both encodings were dictated by the machine rather than chosen for statistical reasons, which is the point the explainer now makes
- **fix**: The QBoost citation was chased to its source. QCi's white paper says "Neven et al. (2009)" and has no reference section; arXiv:2503.11273 says 2012 and its bibliography resolves to ACML 2012, pages 333-348. The paper is now in the team lead's library and its first page confirms the venue and all four affiliations
- **docs**: What "CV" stands for remains **NOT ESTABLISHED** after three independent primary sources: the arXiv paper (84 uses), QCi's shipped source code, and QCi's own white paper (40+ uses). "Continuous variable" is the near-certain reading from adjacency and is labeled an inference wherever it appears. Web search summaries assert it as fact and cannot be traced to any source document
- **test**: `test_no_control_characters.py` covers every tracked text file after a fifth escape-eaten-by-a-parser defect, this one written into the CHANGELOG entry describing the first. On its first run it found a sixth instance already committed in the master plan

- **docs**: **F73 shipped: `docs/explainer/THE_SUBMISSION_EXPLAINED.md`**, the submitted proposal and appendix explained for an 8th grader. 4,826 words explaining a 5,847-word submission, structured like a paper rather than a FAQ. The quantum device appears in section 5 of 7 rather than section 2 where the submission puts it, because a reader who meets Dirac-3 before understanding why fraud detection is hard files the whole thing under the wrong subject (Issues #101-#108)
- **fix**: **The outline claimed three datasets; the submission used two.** `grep` finds SPECTRA zero times in the proposal and appendix -- it is four datasets about steel plants, gas turbines, maintenance and telecom churn, read as prior art for a different argument. Corrected in the document rather than deleted silently, because "there were three datasets" is exactly the small false fact a reader carries away and repeats (Issue #103)
- **fix**: Two unsourced claims caught before shipping, both written from background knowledge rather than from this repository: "September 2013" and "V1 through V28" for ULB, and "the American set" for IEEE-CIS. None appears anywhere in the repo. Removed (Issue #103)
- **docs**: Reading levels are recorded as TWO numbers per section, aimed and measured, after Flesch-Kincaid returned 5.1 to 7.9 against targets of 8 and 9. Both are kept with a note on why they disagree: the formula counts syllables and sentence length and cannot see conceptual load, so it is a floor that catches needless density, not a verdict (Issue #107)
- **process**: **This repository's sessions never write to another repository.** Recorded in CLAUDE.md standing rules, the NOT-do list, workflow invariant 9, a memory file, and a PreToolUse hook with 17 test cases. Written after a session reviewing PR #97 also edited, committed and pushed changes to a sibling repository; every finding was genuine and the boundary was still wrong, because the changes arrived with no review, no tests and no sprint record (PR #99)
- **fix**: The cross-repository guard's first version blocked two legitimate read-only commands within minutes of shipping, because it tested "is this mutating?" and "does it name a sibling?" independently and ANDed the answers. It now requires the sibling to be the TARGET of the write. Seven of its 17 cases assert the guard ALLOWS something; that half is the half that failed (PR #99)
- **fix**: Registering that hook reproduced the exact backslash-b to U+0008 JSON escape that killed two hooks in Sprint 14. The new hook pointed at a path beginning with a backspace character and would have silently never fired. Caught by the path-resolution check, not by inspection (PR #99)
- **docs**: `CLAUDE.md` gained a "Things Claude Should NOT Do" section, every entry a failure that actually happened in this repository. `CLAUDE.md` and `README.md` were also added to the em-dash guard's coverage: the file STATING the no-em-dash rule was not itself checked for it, which is the same shape as a guard that cannot fail (PR #99)
- **feat**: F78 added to the backlog: convert all 13 tracked `.ps1` files to Python that runs on Windows 11 and Linux. Measured rather than estimated -- about 1,260 lines, four hooks with zero OS-specific constructs, and 18 hardcoded `.venv\Scripts\python.exe` paths across 12 files. CI runs ubuntu-latest, so every hook is Windows-only protection today (PR #99)

### 2026-09-14

- **fix**: **BOTH Edit-matcher hooks had NEVER RUN** (F68). `.claude/settings.json` registered them as `...\\hooks\block-...`, where the single backslash before `b` is a JSON backspace escape, so the path resolved to a file that does not exist and Claude Code skipped it silently. Broken since the commit that created them, and the Sprint 13 retrospective had recorded one of them as working. Every submission-document edit that sprint was unguarded. Added `test_hook_registration.py` covering the class rather than the instances, and `block-frozen-history-rewrite.ps1` refusing force-pushes and `prereg-freeze` tag moves (Issue #84)
- **fix**: **`requirements-lock.txt` was UNINSTALLABLE** (F69). A `python==3.12.10` line made pip resolve `python` as a package, find none, and abort before installing anything, so the documented reproduction command failed at step one for every reader. Found by executing the README against a fresh public clone rather than drafting it. A test also crashed on a clean checkout where its twenty peers skipped, making the suite red for an absent dataset rather than a defect (Issue #85)
- **docs**: README rebuilt for a public reader and every maintained figure removed from it: counts, amendment totals and campaign figures now reference their source rather than restating it. The suite counts had already gone stale once inside the same sprint, which is the argument in miniature (Issue #85)
- **fix**: CHANGELOG backfilled for eight missing days covering Sprints 7 to 13, and workflow step 8.1.1 added BEFORE backlog refinement so the gap cannot silently reopen. The currency guard written for it initially PASSED on the eight-day lapse it existed to catch, because the threshold was 14 days; caught only by running the injection (Issue #86)
- **process**: `CHECKLIST.md` split into three files with three change policies: `CHECKLIST-Phase1.md` CLOSED and never updated, `CHECKLIST-Phase2-pre.md` live, `CHECKLIST-Phase2.md` dormant with the six committed experiments frozen (Issue #87)
- **fix**: A dry run OVERWROTE the committed `b3_hardware.json` during F65 verification, replacing 12 real fits and 62 metered seconds of [HW] evidence with three placeholder rows. Restored from git; dry runs now write elsewhere. The "artifact untouched" check that missed it had run before the background job finished, so a mistimed verification read exactly like a passing one (Issue #87)
- **feat**: F48 shell-metacharacter hook, F66 Copilot reviewer-request procedure corrected in the workflow, F65 per-fit artifact writes in the B3 runner, F67 pool-mechanism guard now running on a fresh clone against a committed 0.83 MB int8 fixture (Issues #88 #89 #90 #91)
- **docs**: **ADR-0014 (Evidence Based DB) and ADR-0015 (reference library) both ACCEPTED**, early-innovation status with a 20-paper checkpoint. Neither authorises implementation. Primary-source research on Cyc corrected two things this project had recorded as fact: four composite truth values rather than five, and NO numeric confidence on assertions, so the confidence percentage was our own design and was replaced by GRADE-derived certainty with named reasons (Issues #92 #93)
- **fix**: Two PR reviews on #94 found 20 defects, all addressed. Copilot's first was LIVE: `test_hook_registration.py` left Windows backslashes in resolved paths, so 0 of 7 hooks resolved on POSIX and CI had been red on four consecutive commits. The Claude review found a merge blocker: the dry-run guard was VACUOUS and its replacement was vacuous too, both defeated by the same injection, now fixed with an AST structural assertion (Issue #93)

### 2026-09-13

- **docs**: ADR-0014 accepted. Scope corrected during review: the HSBC glossary is use case ONE, not the boundary. The target is a domain knowledge base over QML, ML, QC and the major QC platforms, which weakened the tractability argument and moved authorship economics to the center of the design (Issue #92)
- **docs**: Reading level ESCALATES one grade at a time to a PhD ceiling, with the postgraduate threshold rather than grade 13 as the signal worth addressing. Escalation overturned my own claim that seven terms were unachievable: "convex" needed one grade, not abandonment, because the 8th-grade gloss contradicts our own appendix (Issue #92)
- **docs**: Named the Evidence Based DB and recorded that private visibility makes inline citation a constraint rather than a preference: a pointer into a private repository does not resolve for the public reader this repository has (Issues #92 #93)

### 2026-09-12

- **process**: **SUBMITTED.** Three files uploaded to the HSBC track: proposal 6 of 6 pages, appendix 3 of 3, team profile 1 of 1, US Letter at 10.0pt; the portal returned "Your submission received." Filed three days before the deadline. The portal renamed the files on ingest to `proposal-1`, `appendix-6` and `team_profile`, recorded so a later reader comparing the portal list against this repository does not find three names matching nothing. Two upload slots remain unused (Issue #81, F10)
- **process**: Requirements-matrix row A5's four residual unknowns answered by the act of uploading: no per-file size cap is displayed; there is no post-upload metadata prompt; the deadline timezone is not shown anywhere in the challenge view and stays unresolvable from the UI, made moot by submitting early; and uploading IS the committing action, in two steps, where staging leaves the counter at zero with a Remove control per row (Issue #81, F10)
- **fix**: The Sprint 13 evidence walk, scoped as a formality, found NINE defects that the 320-test suite caught none of. The comparator was quoted at a size its own rows contradict, 78 against the 91 every `hw_b1_dct` row carries (#77). Six of 92 requirements-matrix rows contradicted the shipped documents, including C5 and C6 pointing at the wrong sections after F55's renumbering (#78). Six more were found in the submission documents themselves, the worst being a three-fold mean spliced into a sentence ending "fold 0" (Issues #77 #78 #79 #80, F10)
- **fix**: Amendment A32: the linear-term spread stated in A31 corrected. A31 said the frozen Hamiltonian's linear terms differ by at most 16.0; the stored per-pool values run 12.0 to 28.0, and 16.0 is one seed's value quoted as the maximum. The resolution argument is unaffected and now stated more conservatively. A proposed fifth correction was backed out on team-lead direction and A32 narrowed to this alone (Issue #77, F10)
- **process**: Sprint 13 closed: Manual Validation passed all four items, retrospective at 16 categories by 4 roles with all six improvements applied, PR #82 to develop and PR #83 to main
- **fix**: **F68: the two guards over frozen artifacts had NEVER RUN.** `.claude/settings.json` registered both Edit-matcher hooks with a single backslash before `block-`, which JSON parses as U+0008 BACKSPACE, so the path resolved to a file that does not exist and Claude Code skipped it silently. Broken since the commit that created them. The Sprint 13 retrospective had recorded one of them as working. Both now demonstrably block, proven by injection; `test_hook_registration.py` asserts every registered path resolves and holds no control character (Issue #84)
- **feat**: F68: `block-frozen-history-rewrite.ps1` refuses force-pushes, history rewrites and moves of the `prereg-freeze` tag. The repository is public and two commits are cited in documents that were already submitted and judged, so a rewrite would break a citation inside a PDF that cannot be corrected (Issue #84)
- **fix**: **F69: executing the README found two defects invisible from the working tree.** `requirements-lock.txt` was UNINSTALLABLE -- a `python==3.12.10` line made pip resolve `python` as a package, find none, and abort before installing anything, so the documented reproduction command failed at step one for every reader. And one test crashed with FileNotFoundError on a clean checkout where its twenty data-dependent peers skipped, making the suite red for an absent dataset rather than a defect. A fresh public clone is now green: 256 passed, 21 skipped, 0 failed (Issue #85)
- **docs**: F69: README rebuilt for a public repository. It previously described a `paper/` path that does not exist and asked, under "Immediate to-do", for challenge PDFs staged weeks earlier. It now leads with what was submitted and that the headline result is a null, then how to retrieve the as-submitted state, how to reproduce with numbers measured from a fresh clone, and what cannot be reproduced and why (Issue #85)
- **feat**: F48: `block-shell-metachar-expansion.ps1` blocks backticks, `$(...)` and `${...}` on a line invoking python, which bash expands before python runs. Scope took three iterations, each correction driven by a false positive against real commands; the final anchor requires python at a command position. Verified across an 11-case injection matrix (Issue #88)
- **process**: Sprint 14 planned and approved: F68 to F72 registered, F39 rescoped from build to investigation-and-ADR, plus the four carried tooling cards. The first sprint of the project with no deadline behind it, finalist notification being mid-November 2026

### 2026-09-11

- **feat**: **Amendment A31 (F57): the hardware-versus-proxy agreement on the frozen pool is FORCED by device resolution, and our claim that it bounded resolution effects was backwards.** QCi documents about 200:1 analog resolution. Measured on all ten pools: the diagonal is 510,705 while off-diagonal entries span at most 20.0, against a resolvable difference of 2,554, so NO coefficient difference in the problem is visible to the device. Quantised at that resolution the minimiser is uniform to 2e-15, and hardware returning uniform is what resolution predicts whatever the true optimum is. The same limit explains B2's 0.83 weight cosine as a device solving a sparsified problem rather than failing to solve it, which makes cardinality-constrained selection the Phase 2 direction that follows from the device's own physics
- **process**: **F37 closed: the repository is PUBLIC**, verified anonymously (HTTP 200, freeze commit 95751b9 resolves, `prereg-freeze` tag intact). Appendix C's reproducibility claim is true as written for the first time rather than promised. Pre-flip scans across all 259 commits found no secret ever committed. The visibility flip is not a commit, so this date comes from the requirements matrix and the sprint summary (Issue #56)
- **process**: F38 closed: proposal 6 of 6, appendix 3 of 3, team profile 1 of 1. The strict xfail markers parked in Sprint 9 fired the moment the documents came back under limit and are gone, as their docstring required (Issue #57)
- **docs**: The LLM adversarial reviews disclosed in the team profile; five wording and framing fixes from the team lead's review; the last two contingent framings of the gate-based arm removed, so the Phase 2 Braket commitment is unconditional; two experience-wording changes applied to both documents
- **process**: Sprint 12 closed: retrospective with all seven approved improvements, three of them made deterministic rather than documented; a Copilot finding on PR #75 that the dispersion split keyed on a field that does not vary; PRs #73/#74 and #75/#76 merged. (Those four PRs record UTC merge times just after midnight on 09-12; the work and the sprint documents place them on 09-11.)

### 2026-09-10

- **feat**: **Block B2 EXECUTED on Dirac-3 [HW] and is the campaign's only positive result at scale** (amendment A24, revised the same day by A25). Eleven fits at 833 continuous variables, 906 metered seconds, after the team lead raised the run ceiling to 2,000 QPU seconds. Against B1's matched `hw_b1_dct` arm on identical seeds: **+0.0256 AUPRC, 95% CI [+0.0203, +0.0310], 10 of 10 seeds**. Unpaired the effect hides inside a 0.030 seed-to-seed standard deviation. It still trails full-feature CatBoost by 0.0440, so the preregistered null holds. Quoting B1's overall mean rather than the like-for-like arm would have inflated the gain from +0.026 to +0.056 (Issue #71, F2b)
- **fix**: **Block B3 was PUBLISHED, WITHDRAWN and RE-RUN the same day** (amendments A22 then A23). The first ladder built its pools on the whole training fold while the [SIM] arm it was quoted against subsamples to 100,000 rows, so "only the solver differs" was FALSE as published. Re-run matched: 12 fits, 62 metered seconds, every cell within 0.0013 of the classical ladder with errors scattering both ways. Recorded as a separate amendment rather than folded into A22, because a published-then-changed figure must stay traceable as such. Cost: 61 wasted metered seconds (Issue #72, F2b)
- **fix**: Amendment A25: an internal four-way review corrected seven published claims in documents that had already passed 300 tests. Every recomputed figure was correct; every defect was in what the numbers MEANT
- **fix**: **F49 (A26): the classical comparator was NOT CONVERGED.** `solve_weighted` stopped on a relative-objective test at 1e-10 against a nearly flat objective, certifying the objective rather than the solution at a KKT residual of 4.6e-05. It now stops on a certified KKT residual at 4.0e-10, and three reported figures move. No internal test could have found it: the figure and the error came from the same solver, so every check agreed with itself
- **fix**: **F50 (A27): two mechanism claims WITHDRAWN**, one written the previous day in response to an internal review and published within hours. The convexity claim was false -- `weak_cls_schedule` selects which feature subsets each learner reads, not the Hamiltonian's order, verified on B2's own pool. And uniform is not the zero-penalty optimum: at lambda=0 equal weights over the 82 perfect learners score below uniform on all ten seeds
- **process**: **F51 (A28): a protocol deviation undeclared for ELEVEN SPRINTS.** G0's preregistered rule commits that below 0.85 mean AUPRC "the pipeline is presumed defective and everything halts". G0 scored 0.8296 and work continued. The gate always read FAIL and nothing was rescored, so the scoring was honest, but reporting a failure is not honouring the consequence attached to it. Also corrected: the claim that Loke et al. was not found before freezing, which the freeze itself disproves
- **fix**: F52 to F56 (amendments A29, A30): prior-work interpretations corrected, document contradictions repaired where B1's variable count read 78 in three places and 91 in two, and the two missing rubric sections added -- "Feasibility and Resource Requirements" and "Validation Plan", together 35% of the Phase 1 weight with no heading for a judge to score against
- **process**: F57 to F63 registered from a second review pass over the updated documents, including two findings where the reviewer was wrong and we are right -- the allocation arithmetic and the shuffled-label control -- recorded so the next reader does not re-raise them
- **chore**: B3 fold-prep caching across k cut the run from 2.0 hours to 31 minutes; B2 implemented as a block in `run_hardware.py` rather than a fourth runner (Issues #71 #72)

### 2026-09-09

- **feat**: **Amendment A21: QCi GRANTED 3,000 seconds of full-access Dirac-3 time.** The A12 free-tier 100-variable ceiling is confirmed as a free-tier limit and no longer binds (Issue #67, F46)
- **feat**: F47, a hard gate before any further metered job: the metered-call wrapper captures the job id at submission and reads cost from the balance. The QPU cost model is validated as `ceil(sum(runtime))` (Issue #68)
- **fix**: **Amendment A20 (F41): the published pool-degeneracy mechanism was WRONG, and the "depth-limited" description of the weak learners was FALSE for the frozen arm.** The submission explained degeneracy by saying depth-limited trees mostly predict the negative class at 0.17% prevalence. The saved pools disprove it: the frozen arm's trees are unbounded, exactly zero learners predict all-negative, and 80 to 84 of the 91 reproduce the training labels exactly. Right observation, wrong cause (Issues #59 #60)
- **fix**: F42: the +0.0319 gain DECOMPOSED against a per-seed comparator. Four learner families instead of one contributes -0.0047 and does not clear the MDE; fit-time class weighting contributes **+0.0328 on 10 of 10 seeds and does clear it**. The gain came from class weighting, not classifier variety (Issues #60 #61)
- **fix**: The gate-report undercount fixed: `score_gates.py` filtered hardware rows on `arm == "cvqboost_hw"` and silently dropped the 10 `cvqboost_hw_mixed` fits, certifying 27 fits / 120.0 s while `results.json` held 37 / 163.0 s (Issues #59 #60)
- **feat**: F43 AUC-ROC reporting, F44 figure-resolution tests, F45 evidence guard, F35 interpretation-layer tests (Issues #63 #64 #65)
- **process**: F40 completed: every absolute filesystem path removed. Doing it surfaced a real reproducibility hole -- `data.py` hardcoded the ULB dataset location, so Appendix C's regeneration promise would have worked on no machine but the author's (Issue #55)
- **process**: Sprint 10 closed (PRs #58/#62) and Sprint 11 planned, executed and closed the same day (PRs #66/#70), taking the suite from 201 to 280 tests. F37 and the fresh-eyes review deferred to Sprint 12 as explicit decisions

### 2026-09-08

- **feat**: **F4 complete: H6, the last preregistered experimental arm, is a measured null.** The QFE phase representation shifts the quantum-minus-classical delta by **-0.0115** against a 0.0268 MDE, negative on 7 of 10 seeds, and the falsifier fired. Reported as "a small negative shift that does not reach claimable size", with that framing fixed in writing BEFORE any result was seen. Three runs, two discarded: 18.9 hours of compute against a 10-minute estimate (Issue #50)
- **fix**: Amendment A19: A18's description of the H6 pool corrected. A18 said the arm used a k=13 sequential pool matching the frozen configuration; `run_h6.py` passes the full feature matrix with no top-k reduction. The code was right and the description wrong, so no figure changed (Issue #50)
- **fix**: Sprint 9 PR #53 findings: two from a Claude review of my own work, one Copilot finding that appendix A.6 described a configuration that was not run, and a CI failure because pypdf and scipy were never declared (Issue #53)
- **process**: Sprint 9 closed: retrospective at 15 categories by 4 roles, improvements 1, 2 and 4 applied and 3 declined, PRs #53/#54 merged, and CHECKLIST.md reconciled after being stale since the Sprint 4 close. Close-out violations recorded rather than smoothed
- **process**: F40 registered and executed: seven team-lead working files and 42 reference files moved out of the working tree before the repository goes public. History deliberately NOT rewritten, on team-lead decision; all 49 moved files verified content-identical against their git blobs before deletion (Issue #55)

### 2026-09-07

- **feat**: F14: the eqc-models feedback package that the QCi letter had promised, three pages and four findings, every claim cited to file and line and verified mechanically (Issue #51)
- **feat**: F16: minimal CI on PRs to develop, with a dirty-tree gate. A test that modifies tracked files now fails the build, which is the Sprint 8 defect that lost an evidence file (Issue #52)
- **feat**: Amendment A18: the H6 representation arm registered and run with the order-matched classical twins the Fourier Wall result requires. The GAM twin's basis handling was fixed and eight cells discarded and rerun; per-cell checkpointing added, and the checkpoint mechanism itself tested, since an untested safety net is worthless (Issue #50)
- **process**: Sprint 8 closed and Sprint 9 planned. F39, the fact database, registered from the Sprint 8 retrospective. Three Copilot review comments addressed, each verified before acceptance; a self-review found the folio heuristic had dropped real content (Issue #47)

### 2026-09-06

- **feat**: **F3 complete: IEEE-CIS under its preregistered protocol**, zero metered seconds and every result [SIM]. 590,540 transactions, rolling origin by month, reduced Deotte recipe with UID excluded. Classical arms: LightGBM 0.5739, XGBoost 0.5028, CatBoost 0.4795. CVQBoost proxy 0.0571 tuned at the six features the A12 ceiling permits (Issues #43 #44 #45)
- **feat**: The H3 regime ladder, 12 cells, **answers against our own interest**: the slope is NEGATIVE at -0.006 AUPRC per feature, so lifting k from 5 to 17 does not close the gap. This contradicts a claim made mid-sprint that the ceiling was the binding constraint, and the contradiction is recorded rather than smoothed (Issue #46)
- **feat**: The matched-feature control, the most valuable thing built in Sprint 8: CVQBoost's 0.0571 against a classical 0.5739 reads as hardware failure until the same LightGBM is given the SAME six features and falls to 0.0734. Every model is starved there, and the quantum arm attains 85% of that constrained ceiling (Issues #44 #45)
- **fix**: **Amendment A15: a FABRICATED comparator figure corrected.** The value 0.7688, quoted in four places as the frozen pool's AUPRC at k=6, appears in no results artifact. It is the mean of an exploratory five-seed run carried into a ten-seed write-up without recomputation, and the true ten-seed mean of 0.7629 inverts the conclusion it was carrying
- **fix**: **Amendment A17: a PROTOCOL VIOLATION live since Sprint 6, with every A11 and A13 figure recomputed.** Three fold builders called the loader directly and never deduplicated, training on 284,807 rows against every comparator's 283,726 for two sprints. Stated plainly because the correction moves the result in our favor: the paired difference rises from +0.0198 below the MDE to **+0.0319 on 10 of 10 seeds, exceeding it**. The violation was MASKING the effect. The mechanism is measured, not assumed: rows in duplicate groups carry a 1.73% fraud rate against the dataset's 0.17%
- **fix**: F36 FAILS its own acceptance criteria and is recorded as a failure. The Lua float filter works, but the problem it was built for did not exist: measured as vertical extent rather than character count, every appendix page already filled its text block. The lasting fix was to the instrument -- `page-fill-report.py` now measures points and excludes the folio (Issue #48)
- **process**: F37 registered as a SUBMISSION BLOCKER: Appendix C cites a GitHub repository that was private, so an anonymous request returned 404 and the appendix cited a dead link

### 2026-09-05

- **feat**: **F33: a tuned four-family pool reaches 0.7827 AUPRC**, against 0.7565 untuned. The reported comparison is the matched one, the frozen pool rebuilt at k=6 on the same splits: **+0.0198, positive on 9 of 10 seeds, below the 0.0268 MDE** (superseded the next day by A17, which raises it to +0.0319 on deduplicated data). The finding worth carrying forward is that the accuracy came from the LEARNERS, not the optimizer: solved-minus-uniform on the tuned pool is only +0.0043 (Issues #38 #39 #40)
- **feat**: F34: eight spend-guard property tests, each asserting the property whose violation caused a real defect, and each verified to FAIL on the original code rather than trusted to pass (Issues #38 #39)
- **fix**: **Amendment A12: the free-tier variable ceiling established empirically, and an earlier inference declared WRONG.** A 312-variable continuous job returned HTTP 400 server-side before any billing: a separate free-tier ceiling of 100 variables binds continuous jobs directly, where we had inferred the constraint was our own budget. Zero metered seconds spent establishing it
- **feat**: Amendments A13 and A14: the tuned mixed pool registered as a labeled exploratory analysis BEFORE the ten-seed run with its scope limits stated, and the matched-k comparator recorded
- **feat**: F31 (amendment A11): a mixed-family pool moves the CVQBoost optimum off uniform. F32: the ceiling found, the proposal corrected, and the first six metered fits with hardware predictions persisted (Issues #31 #35)
- **fix**: All nine Claude review findings on the Sprint 7 PR addressed, and chasing one of them surfaced the A17 protocol violation. `comparators.py` had been built to forbid order-mismatched comparisons and then never imported, while the tuned-pool module kept differencing across k=6 and k=13 by raw subtraction
- **process**: Sprint 5 closed (PRs #29/#30) and Sprint 6 closed (PRs #36/#37); Sprint 7 planned and executed. ADR-0013 accepted

### 2026-09-04

- **process**: F30 registered (HOLD): concurrent Dirac-3 submission with a bounded in-flight window and a crash-safe job ledger; full card drafted for review
- **process**: F29 registered: proxy test of the team lead's observed CVQBoost sample-size insensitivity (250k to 7M rows equivalent in prior work), with the linear-scaling mechanism from the Sprint 4 degeneracy finding as the candidate explanation; labeled exploratory, zero metered, include only if the evidence supports it
- **process**: Sprint 4 closed: PR #21 merged to develop, develop to main via PR #22; carry-forward branch feature/20260904_Sprint_5 created; SPRINT_4_SUMMARY.md written (three-doc rule); master plan rolled (F22/F21/F2-first-blocks/F7 pruned, F2b carries the remaining hardware); CHECKLIST reconciled; sprint_status rolled to Sprint 5
- **fix**: All 17 PR #21 review findings addressed (Copilot 2, Claude 15) with amendment A8; most serious was unparseable billing reading as zero spend, which had made the hardware budget guard blind; 22 tests green including new spend-accounting guards

### 2026-09-03

- **process**: Sprint 4 retro improvements applied (all as recommended): evidence-after-the-fact verification rule, external-review pass on evidence documents, audience-first deliverable rule, plain-language companion as deliverable, search-space containment check, re-read-before-write defect class; score_gates.py known-answer tests recorded into F8
- **feat**: Prereg amendment A7 approved: protocol-sensitivity ladder (S1 metric definition, S2 duplicate-retained, S3 split ratio) as labeled exploratory cells; G0's criterion and FAIL outcome unchanged; prediction persistence added as the enabling change (also unblocks the H1b paired BCa)
- **process**: Headline framing DECIDED (Class-2): production-bound program spine -- deployable system today, rigorously mapped search for what improves it, stated expectation with its reasons, explainability as a named advantage, concrete transfer path
- **process**: F25 registered (Phase 2 HOLD): non-convex cardinality-constrained CVQBoost on Dirac-3's integer solver, the formulation where no exact classical proxy exists
- **process**: F26 (cost-based operating points), F27 (production-trial design), F28 (explainability thread) registered per team-lead direction that the proposal read as production-bound; results memo recommendation reframed to the system/program spine
- **feat**: Hardware blocks B1 + G0b EXECUTED [HW] on team-lead approval: 27 Dirac-3 fits, 120 QPU s, 0 failures, 0 retries. **G0b PASS** (Spearman 0.900 vs 0.5 gate). H4 structural attribution measured: hardware minus exact proxy = -0.0010 AUPRC [CI -0.0032, +0.0012], weight cosine 0.975-0.999. H1b on hardware: -0.0399 [CI -0.0571, -0.0227] vs CatBoost matched-13, reported as the preregistered null (Issue #19)
- **feat**: F22 section-6 proxy tuning complete (100 trials); starting config retained by the validation-AP rule; tuned lg config lifts the score-health quarantine; G0b ranking produced (Issue #17)
- **feat**: Amendment A6 (score-distribution health flags); F21 research memo: G0's 0.85 band not corroborated by primary sources; A7 sensitivity cells proposed (Issues #17 #18)
- **process**: F23 (F4 prep: QFE phase recipe + twin scaffolding) and F24 (F5 prep: SPECTRA in-segment machinery + B4 request) registered at priority 15 per the team lead's Sprint 4-8 roadmap
- **process**: Sprint 3 closed: PR #13 merged to develop, develop to main via PR #16; carry-forward branch feature/20260903_Sprint_4 created from the Sprint 3 head; SPRINT_3_SUMMARY.md written (three-doc rule); master plan rolled (F1/F18 pruned); CHECKLIST reconciled; issue #15 closed; sprint_status rolled to Sprint 4

### 2026-09-02

- **fix**: Sprint 3 validation findings addressed: lg proxy rows quarantined from all report tables (degenerate scoring, 99.8% identical scores); dct tie caveat noted; root cause diagnosed (lambda=2*n_train near-uniform weights + unweighted weak learners on 0.17% positives); fix path = preregistered section-6 proxy tuning, registered as F22 (blocks G0b); F21 registered (duplicates-methodology + feature-engineering research re G0)
- **feat**: Prereg amendment A5: MDE refined to measured paired-delta value 0.0268 (team-lead approved at validation)
- **feat**: F1 classical evidence campaign complete [SIM]: 110 results.json rows (4 arms x 2 feature sets x 10 seeds + 30 proxy rows); G0 scored as committed = FAIL (tuned-XGB full mean test AP 0.8296 vs 0.85 floor); A3 side-by-side = full-pair selected on validation (0.7816 vs 0.7803); paired proxy-vs-best-GBDT delta -0.0415 [CI -0.0608, -0.0222]; measured MDE(10) 0.0268; budget table published; zero metered seconds (Issue #15)
- **feat**: F18 complete: Dirac-3 notes mined, 10 dispositions, zero amendments required (Issue #14)
- **feat**: Prereg amendment A4 (Sprint 3 analysis-code additions registered); CVQBoost proxy pipeline (exact Hamiltonian + FISTA, known-answer tested); campaign runner; gate scorer; ADRs 0005-0010; hooks ported; ARCHITECTURE/TESTING_STRATEGY/VELOCITY_LOG docs; B1+G0b hardware request PREPARED not executed (Issue #15)
- **process**: F19 registered (priority 13, team-lead request): draft submission PDFs to QCi at next sprint's end, gated on a pre-send confidentiality scan; team lead sends
- **process**: Sprint 2 closed: PR #2 merged to develop, develop to main via PR #12; carry-forward branch feature/20260902_Sprint_3 created from the Sprint 2 head; SPRINT_2_SUMMARY.md written (three-doc rule); master plan rolled (F11/F12/F15 pruned); CHECKLIST reconciled; sprint_status rolled to Sprint 3
- **feat**: Preregistration amendments A2 (variable-count formula corrected to the sequential-strategy count, verified against eqc-models source + FourierWall2 measured runs; documented 949 device ceiling cited) and A3 (full-pair CVQBoost build side-by-side option via proxy at zero metered seconds, preregistered selection rule, Linux/WSL2); qubo_vars corrected in the same commit per section 11 (team-lead approved)
- **process**: F18 registered (priority 11, before hardware blocks): mine the team lead's Dirac-3 integration notes (qml-unlocked/DIRAC3.md) with a 10-point checklist; binding decision recorded: freeze honored, protocol-touching items enter only as dated amendments

### 2026-09-01

- **fix**: Dual PR review findings addressed (Claude review 10 findings, Copilot review 2 overlapping; PR #2): manifest tool moved to scripts/ (out of the frozen experiments/src), rewritten with portable logical keys, paths sourced from frozen data.py, hard-fail verify (missing/extra/changed all FAIL); .gitignore negation repaired so MANIFEST.json is actually committed; retrospective guide and enforcing docs unified at 16 categories with template sections 15-16; Sprint 2 retro renumbered to canonical order; SPRINT_PROCESS calendar reconciled (process sprint inserted); stopping criteria 4/7 precedence rule (results-invalidating defects always stop); sprint_status plan_approved corrected and update-sprint-status.ps1 extended (dotted-path sections, unknown-key hard error, null typing); Copilot-review request procedure documented in workflow 7.7
- **process**: Merge rule clarified by the team lead: all PR merges at every level are team-lead-only (SPRINT_PROCESS.md)

### 2026-08-30

- **process**: Sprint 2 dispositions applied: statistical-review checklist, CHANGELOG policy, data manifest + verifier, QUALITY_STANDARDS, secrets ADR-0011; F1-sprint items recorded in master plan; CI to backlog as F16 (Issue #10)
- **docs**: ADR system adopted with ADRs 0001-0004 (freeze governance, proxy-as-structural-control, dataset provenance, branch/carry-forward) and Preregistration-touchpoints template section (Issue #10, F15)
- **docs**: Portal verified; submission mechanism is 5-slot file upload; team-profile-as-PDF plan added (A5b); Braket resolved as [PROJ] plan + team-lead expertise (Issue #11, F12)
- **process**: Full sprint execution doc set adapted from spamfilter-multi (workflow, checklist, stopping criteria incl. Criterion H, retrospective 14x4, backlog refinement, planning, master plan); defined-scope rule adopted
- **feat**: Preregistration v1.1 FROZEN (commit 95751b9, tag prereg-freeze, amendment A1); analysis code frozen at same commit (PR #1)
- **feat**: metrics.py at v1.1 statistical spec (stratified BCa 2000, paired identical-index deltas, across-seed t-interval, MDE, Wilson, equal-mass ECE, dual operating points); 11 known-answer tests (PR #1)
- **feat**: Data loaders + validation for ULB, IEEE-CIS (float32), and four SPECTRA sets; Kaggle OAuth wired; datasets staged (PR #1)
- **feat**: Pilot variance run [SIM]: ULB 10-seed mean AP 0.8268, seed SD 0.0243, MDE(10) 0.0242 (PR #1)
- **chore**: Private remote, main/develop/feature branch model, draft-PR lifecycle, pre-commit confidentiality hook (verified blocking), Copilot review instructions (PR #1)

### 2026-08-29

- **docs**: Requirements matrix verified against all four official challenge PDFs; reference library read in full; preregistration v1.0 through dual-model review + methods research to v1.1; QCi sponsorship letter v3 with Outlook draft
