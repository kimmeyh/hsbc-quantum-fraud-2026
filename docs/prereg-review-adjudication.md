# Preregistration v1.0 to v1.1: Review Adjudication

2026-08-30. Inputs: independent review A (Sonnet, items S1-S15), independent review B (Opus, items O1-O15), full-paper design implications (F1-F5, docs/references.md), methods research recommendations (R1-R10 plus checklist items, docs/research-baselines-best-practices.md). Adjudicated by Fable; every decision below is applied in PREREGISTRATION.md v1.1 unless marked REJECT/DEFER.

Decision key: ACCEPT = applied as proposed. MOD = applied with stated modification. REJECT = not applied, reason given. Overlapping proposals are adjudicated once.

| # | Items | Topic | Decision | Resolution |
|---|---|---|---|---|
| 1 | S2, O1 | Multiplicity / forking paths | MOD | ONE primary confirmatory endpoint; all other cells labeled exploratory with Holm-adjusted intervals, K fixed at freeze. MODIFICATION vs O1: primary endpoint is ULB, not IEEE-CIS. Reason: the primary endpoint must be evaluable regardless of the QCi grant and must not depend on the riskiest feature-engineering pipeline; ULB has data on disk, corroborated reference bands, and both free-tier and full configs. IEEE-CIS remains the scaling/regime headline in exploratory status. |
| 2 | O2 | CI machinery underspecified | ACCEPT | Paired stratified BCa bootstrap, 2,000 resamples (per DiCiccio-Efron), identical resample indices applied to both arms' prediction vectors. Decision rule for the primary endpoint: t-interval on the across-seed mean of per-seed paired delta-AP (10 seeds) excludes zero; per-seed paired BCa CIs reported as supporting evidence. |
| 3 | S3, O9 | Seeds, power, temporal variance | MOD | 10 seeds for the primary cell (matches SMU, removes a comparability objection), 5 for exploratory cells; splits varied along with seeds (Bouthillier). A minimum-detectable-effect statement is computed from pilot seed variance and margins below the MDE are reported as indistinguishable. Temporal protocol: 3-fold rolling origin on IEEE-CIS and Sparkov; ULB temporal is a single-split SENSITIVITY CHECK only (the dataset spans 2 days; research R2). REJECT S3's adaptive seed escalation: adaptive stopping is itself a researcher degree of freedom. |
| 4 | S4, O4, O5 | Proxy fidelity and tuning fairness | ACCEPT | The proxy is now DEFINED: the identical CVQBoost QUBO objective solved classically (non-negative ridge / SLSQP, as in the QCi paper's own solver comparison), which is simultaneously the H4 structural control. New gate G0b: top-3 and bottom-2 proxy-ranked configs re-run once each on hardware (5 fits, budgeted); Spearman rho < 0.5 means proxy-selected configs are declared non-transferable and hardware results carry no tuning claim. Tuning Budget Equivalence table (trials, fits per trial, CPU-hours per arm) recorded and published. |
| 5 | S5, O6 | Feature-count confound | ACCEPT | Matched-feature comparison (identical top-k set, all arms) is the primary fairness comparison; full-feature GBDT reported separately as the classical ceiling; MI selection fit inside training folds only. |
| 6 | S6, O7, R5, R6 | IEEE-CIS feature engineering and leakage | ACCEPT | The reduced Deotte recipe is preregistered explicitly (D-column normalization; single UID from card1+addr1+D1, UID itself excluded from the model; named aggregate list; frequency encodings; V-reduction by missing-pattern grouping; no client-mean post-processing; no blending). Leakage controls section: every transform fit on train only inside a pipeline; temporal aggregates use only rows before the split boundary; time-consistency and adversarial-validation filters (thresholds stated) inside training folds; shuffled-label positive control; deduplication with counts reported. |
| 7 | S7, O8 | SPECTRA scope and budget allocation | MOD | SPECTRA declared in the datasets table as an external-validity replication block; its results are never counted as fraud evidence. B4 cut from 35 to 15 fits (3 strongest cells x 5 seeds); freed seconds go to G0b, an expanded B3, and the H3 ladder. Granted-seconds spend priority: B3 > B2 > H3 ladder > B4 > contingency (B1/B5 run on the existing balance regardless). |
| 8 | S8 | Threshold objective and drift staleness | ACCEPT | Threshold = max F1 on validation, plus a second operating point at a fixed alert budget (card precision at k, per the Fraud-Detection Handbook); validation-to-test threshold drift reported as a robustness metric under temporal protocols. |
| 9 | S9 | H1b evaluability if grant absent | ACCEPT | Explicit fallback: without hardware, the primary endpoint is evaluated on the proxy and labeled [SIM]; the [HW] confirmatory version is deferred to Phase 2 with this grid cited as committed. Gate status is never ambiguous. |
| 10 | S10, O12 | Structural control specification | ACCEPT | Control = identical weak-learner pool with weights fit by non-negative ridge (lambda tuned over the same grid as CVQBoost's lambda_coef) plus a non-negative sparse variant; H4's attributable delta is CVQBoost minus the BEST control. |
| 11 | S12, O14, R3 | SMOTE and the SMU reproduction | ACCEPT | H1a runs the source paper's protocol (train-only SMOTE) as a labeled reproduction, reported separately; the house protocol is weighting-only; the two never share a comparison table. An uncorrected (no weighting) variant is reported alongside for calibration honesty (research checklist 13). |
| 12 | S13, O15 | Amendments, scoring, failure handling | ACCEPT | Amendment vs deviation defined (no gate-criterion changes, no removing observed hypotheses); gate table with Prediction/Outcome/Evidence-tag/Amendment columns published including nulls; analysis code frozen at the same commit; errored hardware solves retried at most twice with retry counts reported, three failures = reported as failed, never dropped; results.json schema fixed. |
| 13 | S14, O3 | H3 statistics | ACCEPT (O3 version) | Cross-dataset Spearman dropped. H3 becomes a within-dataset dose-response ladder: preregistered feature counts k in {5, 9, 13, 17} on ULB and IEEE-CIS, slope of delta-AUPRC vs k with seed-clustered CI; descriptive-only if fewer than the stated minimum cells run. |
| 14 | S15 | Compound falsification | ACCEPT | A stated kill criterion for the central theory, with the pre-committed fallback framing (regime map / boundary) if it fails. |
| 15 | S1 | H5 HARKing disclosure | ACCEPT | H5 and the starting configuration are labeled exploratory-derived, confirmatory-tested; provenance (prior sweep on partially reused SPECTRA data) disclosed; any re-tuning for fraud segments uses its own held-out split. |
| 16 | O10 | In-segment negative controls | ACCEPT | Segments defined on train-only statistics by frozen predicates computable without labels at inference; matched random-segment control (same size and base rate) with the reported edge = in-segment minus random-segment; minimum 50 test positives per scoreable segment cell. |
| 17 | O11 | H1c operationalization | ACCEPT | log(time) vs log(rows) slopes over a preregistered row ladder at fixed features; pass = CVQBoost slope below every GBDT slope with non-overlapping 95% CIs; hardware stated; GBDTs timed at 1-core and all-core; CVQBoost time includes weak-learner construction. |
| 18 | O13 | G0 specification | MOD | G0 = mean AUPRC across the 10 stratified seeds, full features, weighting-only. MODIFICATION from research findings: pass bar set at 0.85, the bottom of the corroborated clean-protocol band (0.85-0.88); TrueLoop's 0.8937 kept as an unverified-protocol reference, not the gate. G0c fixes ULB preprocessing choices (Time retained as feature, log-Amount) in advance. |
| 19 | F1-F5 | QFE, JOINT twin, phase-active arm, routing, citation discipline | ACCEPT | As specified in docs/references.md: exact Fourier Wall phase recipe; trained-frequency GAM/GA2M/JOINT twins required in every H6 cell; the Braket arm uses a phase-active (sandwich/entangling) encoding with phase-complexity, Berry-connection, and mode-MI diagnostics reported; route-and-blend constraints for the hybrid section; mechanism-not-performance citation rule. |
| 20 | R1, R2, R4, R7-R10 + checklist | Metrics, splits, budget anchors, statistics, calibration, Sparkov | ACCEPT | AP computed step-wise with tie-frequency check; prevalence beside every AUPRC; no cross-dataset comparison or averaging; IEEE-CIS temporal = GroupKFold by month; Sparkov = shipped split, declared a pipeline-correctness check only; tuning budget 100 trials with literature anchors cited and the 60-trial folklore banned; paired tests per R8 with no omnibus at N=3; calibration on held-out fold, Platt when positives are few (ULB), isotonic where supported (IEEE-CIS), equal-mass ECE with reliability diagrams, Brier only as a joint score; NaN passthrough for GBDTs, library categorical handling and CatBoost boosting_type set explicitly, environment pinned. |
| 21 | User directive | De-personalization | ACCEPT | All process references to a named person replaced with "the team lead"; approval and sign-off language made role-based. |

Follow-up engineering task created: `experiments/src/metrics.py` must be updated to the v1.1 statistical spec (BCa at 2,000 stratified resamples, Wilson intervals, equal-mass ECE, threshold operating points) before Stage 3 runs.
