# Preregistration v1.1 (FROZEN)

Status: FROZEN, approved by the team lead 2026-08-30. Supersedes v1.0 after a structured review: two independent model reviews, a full-paper reading of the six methodology references, and a primary-source methods research pass (adjudication record: `docs/prereg-review-adjudication.md`; research report: `docs/research-baselines-best-practices.md`). From this point, changes are recorded as dated amendments per section 11, never silent edits. Analysis code in `experiments/src/` freezes at the same commit.

## Amendment log

- 2026-08-30 (A1): Freeze recorded. Freeze commit `95751b9`, tag `prereg-freeze`. This amendment adds only this log line; no protocol content changed.
- 2026-09-02 (A2): Variable-count formula corrected (implementation-accuracy fix, team-lead approved). Section 10's `vars = n + C(n,2) [+ C(n,3)]` overstates by n for the build we run: the sequential weak-classifier strategy (mandatory on Windows; used for all prior measured evidence) caps pairs at the top-|correlation| n(n-3)/2 (eqc-models `_build_weak_classifiers_sq`), giving true totals C(n,2) at schedule 2 and C(n,2)+C(n,3) at schedule 3. Confirmed exactly by FourierWall2 measured hardware runs (n=15: 105 vars at schedule 2, 560 at schedule 3). Device ceiling: QCi documents 949 (Dirac-3 Developer Beginner Guide, "device limit (currently 949)"), replacing the ~940 estimate; largest fit verified by us remains 816 (n=17, schedule 3). No gate, budget, bound, or cell changes: free tier n<=13 and device n<=17 both stand under either formula (n=18 exceeds 949 under both builds). `qubo_vars` in experiments/src/data.py corrected in this amendment's commit per the section 11 analysis-code rule; section 10's formula line updated with an (A2) marker.
- 2026-09-02 (A3): Full-pair CVQBoost build added as a preregistered side-by-side option (team-lead directed: "the untested weak learners could prove to be an additional advantage"). The multi_processing strategy (Linux/WSL2 only; fork-based) builds ALL C(n,2) pairs where sequential builds the top-correlated n(n-3)/2, i.e. n additional weak learners and n additional QUBO variables. Bounds hold for both builds (free tier: n=13 full-pair = 91 <= 100; device: n=17 full-pair = 833 <= 949). Procedure: (1) both builds are constructed on identical training data with otherwise identical configuration (same weak_cls_type, params, seeds) and compared via the PROXY solve (ADR-0002 non-negative ridge) on VALIDATION AP across the 10 primary seeds, ULB matched features -- zero metered seconds; (2) the build with higher mean validation AP is selected BEFORE any test-set evaluation and applied uniformly to every CVQBoost cell (proxy and hardware); the non-selected build is reported as exploratory; (3) an optional hardware confirmation (one fit per build, same seed/config) may be charged to the section 10 contingency line, gated on Criterion H team-lead approval like every metered run; (4) every results.json row records the build (`pair_build`: sequential | full) inside its config; proxy and hardware always use the identical pool, preserving G0b and the H4 structural-control identity. This amendment adds an option; it changes no gate criterion, no budget, and no existing cell.
- 2026-09-02 (A4): Sprint 3 analysis-code additions registered (section 11: analysis-code changes are dated amendments). Additions: run_classical.py (F1 campaign runner: dedupe-before-split per 5.7, tuning studies per section 6, per-seed refits with dedicated-validation early stopping per section 4, results.json rows per section 11 schema) and the Sprint 3 proxy/QUBO modules as they land, all registered in this log. tune.py is UNCHANGED (a suspected LightGBM API bug in its fit call proved to be the pinned LightGBM 4.7's current API -- `eval_X`/`eval_y` -- and was left as frozen after a smoke-test round trip). No search space, trial budget, gate, split, arm, or statistic changed.
- 2026-09-02 (A5): MDE refined per the preregistered refinement path (section 8.3), team-lead approved at Sprint 3 Manual Validation ("amend to measured MDE and noted in amendment"). The pilot raw-variance MDE(10) = 0.0242 is superseded by the measured PAIRED-delta MDE(10) = 0.0268, computed from the seed SD (0.0269) of per-seed proxy-CVQBoost-minus-best-GBDT deltas on identical test rows (10 seeds, matched features; gate_report.md 2026-09-02). Observed margins below 0.0268 are reported as indistinguishable. No gate criterion changes; this sharpens the reporting threshold only.
- 2026-09-03 (A6): Score-distribution health flags added to metrics.summarize (analysis-code amendment; Sprint 3 retro improvement 1, team-lead approved as recommended). Every row now carries `score_health` = {n_distinct, mode_share, warn}; WARN when the modal score covers > 90% of rows or fewer than 50 distinct scores exist. Reporting-only: ranking metrics are unchanged; the flag marks rows whose operating-point and calibration numbers are weak evidence (the degeneracy found at Sprint 3 validation). No gate, budget, split, arm, or statistic changed. Sprint 4 analysis-code additions (tune_proxy.py, the section-6 tuning harness; build_pool generalized to explicit weak_params/lambda) registered here.
- 2026-09-03 (A7): Protocol-sensitivity ladder added as LABELED EXPLORATORY cells (team-lead approved at Sprint 4 Manual Validation). Motivation: F21 primary-source research (docs/research-ulb-baseline-protocols.md) established that G0's 0.85 threshold derived from a literature band no primary source corroborates. **G0's criterion and its FAIL outcome are UNCHANGED and stay reported as committed** (section 11 forbids changing a gate after observation); these cells quantify the protocol differences involved and are never substituted for the primary cell. Cells, ULB, tuned XGBoost, 10 seeds, zero metered seconds: **S1 metric sensitivity** -- recompute test AUPRC on the identical predictions using trapezoidal auc(recall, precision) alongside step-wise AP; **S2 duplicate-retained sensitivity** -- rerun the frozen pipeline with the 1,081 exact duplicates retained before splitting, all else identical (this produces the first published measurement of the dedupe effect on ULB AUPRC); **S3 split-ratio sensitivity** -- rerun at 80/10/10, preserving a dedicated validation fold for early stopping. Enabling change: the refit and hardware writers persist per-row predictions (also unblocks the per-seed paired BCa the prereg lists as H1b support). Also registered here: Sprint 4 analysis-code additions run_hardware.py (metered runner with per-fit fidelity records), lambda_sweep.py (zero-metered regularization sweep), and the score_gates.py hardware/G0b/H1b-hardware reporting blocks.
- 2026-09-04 (A8): Sprint 4 review-fix registration (analysis-code amendment; no protocol content changed). A 15-finding independent code review of PR #21 corrected safety and provenance defects in the metered runner and the gate scorer: unparseable billed seconds are now charged a conservative estimate and flagged (`status: ok_unmetered`, `metered_seconds_parsed: false`) instead of counting as zero spend; the block cap bounds the call it precedes; sizing errors record their attempt and match a wider vendor-message set; temporal rows record `seed: null` (temporal_split ignores the seed); a post-call error no longer aborts the remaining approved calls; B1 config hashes are verified against the live ranking before any metered run. In score_gates.py: hardware-to-proxy pairing is by `config_hash` rather than label string surgery; the A5 MDE is a single named constant; the hardware H1b cell is selected on VALIDATION AP (repo-wide selection rule) rather than test AP; G0b reports its p-value and refuses to score a degenerate (non-finite) Spearman. In tune_proxy.py: pool cache keys carry the FIXED settings and a build version; refit dedupe keys carry `config_hash`; the starting-config record's string-valued fields are handled. NO reported figure changed as a result (verified by regenerating gate_report.md); 22 known-answer tests green, including new guards on spend accounting.
- 2026-09-04 (A9): Prediction-store keying corrected (analysis-code amendment; no protocol content changed). Predictions persisted under A7 were keyed by (config_hash, seed, protocol). Hardware and its exact proxy DELIBERATELY share a config_hash -- that pool-and-objective identity is what G0b and the H4 solver-fidelity comparison rest on -- so the key collided and the proxy backfill silently overwrote the hardware prediction files. Consequence caught by adversarial review before submission: the cost report showed byte-identical operating points for the hardware and proxy cells, and the draft carried [HW] tags on proxy-derived figures. Fix: `arm` is now part of the prediction key across every writer and reader; the affected files were deleted and regenerated. Reporting consequence recorded honestly: per-transaction hardware scores from the Sprint 4 campaign are NOT recoverable (Dirac-3 weights were never persisted), so operating-point and per-seed-bootstrap figures for the CVQBoost arm are tagged [SIM] and identified as proxy-derived, with the measured hardware-minus-proxy delta (-0.0010, CI containing zero) cited as the justification for the substitution. Aggregate hardware metrics in results.json were never affected.
- 2026-09-04 (A10): Hardware prediction artifacts placed under version control (analysis-code/provenance amendment; no protocol content changed). Per-transaction Dirac-3 scores cannot be regenerated without spending metered QPU seconds, so they are now tracked in git rather than ignored as intermediate data; classical and proxy predictions remain ignored because they rebuild deterministically and free (verified: 80 classical and 10 proxy cells reproduce their recorded AUPRC exactly). Rationale recorded after the A9 collision destroyed the Sprint 4 hardware scores, which neither git nor the nightly backup could restore. Future hardware runs persist and commit their predictions at write time.
- 2026-09-04 (A11): Mixed-family weak-learner pool registered as a LABELED EXPLORATORY analysis (team lead approved 2026-09-04). Sprint 5 established that the frozen pool is degenerate: off-diagonal Gram entries average 170,234.4 against a diagonal of 170,235, so any two of the 91 depth-limited trees agree on 99.999% of training rows, the solved optimum sits within 1.6e-07 of uniform, and the apparent +0.0022 AUPRC over uniform weights is TIE-BREAKING among transactions the pool cannot separate rather than optimization. No optimizer, quantum or classical, can weight interchangeable learners usefully, so the frozen configuration cannot test whether Dirac-3 does useful work. Loke et al. (ICAART 2026) report mean AUC-PR above 0.8 on the same Dirac-3 hardware and the same benchmark family using a heterogeneous pool of k-nearest neighbours, linear discriminant analysis, logistic regression and XGBoost, against our 0.767. This amendment registers a mixed-family pool, built by concatenating per-type H matrices on a shared train fold (eqc-models' `weak_cls_type` accepts one value per build, so heterogeneity requires concatenation), evaluated on the SAME splits, seeds, metric and statistical procedures as the frozen arms. SCOPE AND LIMITS, stated before running: this is exploratory and is NOT a preregistered gate; H1b and every other gate keep their committed scoring and are NOT rescored; the mixed pool is a new arm reported alongside the frozen one, never a replacement for it; and any difference is tested against the A5 minimum detectable effect of 0.0268 rather than reported as a win on sign alone. A capability pre-flight on one seed at schedule 1 (52 learners) measured the optimum moving off uniform (L1 8.0e-08 to 2.9e-02, max weight 1.0552x uniform, AP difference +0.0178), which is the hypothesis this amendment registers for testing at full scale, not a result.
- 2026-09-05 (A12): Free-tier variable ceiling established empirically (measurement record; no protocol content changed). A2 stated that QCi's documented 949 limit is a sum over `num_levels` applying to integer-encoded jobs and therefore does not bind our sum-constrained continuous runs. That remains correct, but the inference drawn from it -- that the binding constraint was our own budget rather than a device ceiling -- was WRONG. Submitting a 312-variable continuous degree-2 job during the F32 block returned HTTP 400 server-side, before any billing, with the message: "Number of variables '312' in problem is greater than the free-tier device limit '100' for polynomial with degree '2'". A SEPARATE free-tier ceiling of 100 variables binds continuous jobs directly. No earlier run met it because the frozen configuration is 91 variables, just under the ceiling. Zero metered seconds were spent establishing this: the refusal is server-side. Consequences recorded: the frozen 91-variable pool fits; a four-family mixed pool fits only up to k=6 (60 variables); the F32 block was resized from k=13 to k=6 accordingly, and a seed-42 check confirmed the optimizer still does real work at that size (L1 from uniform 0.0204, max weight 1.040x uniform, AP difference +0.0036). The proposal sentence asserting a budget-rather-than-ceiling constraint was corrected. Evidence: experiments/results/free_tier_limit_evidence.json.
- 2026-09-05 (A13): Tuned mixed pool registered as a LABELED EXPLORATORY analysis (extends A11; team lead approved 2026-09-05). A11 established that a heterogeneous pool moves the CVQBoost optimum off uniform, but its absolute AUPRC (0.7565) sat BELOW the frozen single-family pool's (0.7681) and well below the >0.8 Loke et al. (ICAART 2026) report on the same hardware and benchmark family. A13 registers the test of whether that remaining gap is learner QUALITY and tuning rather than pool composition. The specific intervention is IMBALANCE HANDLING AT FIT TIME (`class_weight='balanced'` passed to each weak learner as it is built), which no prior control varied: the Sprint 5 class-weighted control reweighted the ENSEMBLE OBJECTIVE and left the learners themselves untouched, which is why it could not move a degenerate pool. Weighting a learner during its own fit is a different operation, and at 0.17% prevalence it is what decides whether a depth-limited learner predicts anything but the negative class. PROTOCOL: two stages, with a seed-42 sweep over four candidate hyperparameter sets selecting on VALIDATION AP only (the repo-wide selection rule; test AP is not read at that stage), then the selected configuration over the same ten seeds, same splits, same metric and same statistical procedures as every other arm. Pool size is held at k=6, 60 variables, inside the A12 free-tier ceiling of 100. SCOPE AND LIMITS, stated before the ten-seed run: exploratory, NOT a preregistered gate; H1b and every other gate keep their committed scoring and are NOT rescored; any difference is tested against the A5 minimum detectable effect of 0.0268 rather than reported on sign alone; and the Loke comparison is a DESIGN comparison, never a claim to have reproduced their number. The sweep's own outcome is recorded whichever way it falls: a configuration that fails to clear the frozen pool is a result about the formulation, not a failed experiment.
- 2026-09-05 (A14): Matched-k comparator recorded for the A13 tuned pool (analysis amendment; no protocol content changed). The A13 tuned pool runs at k=6 (60 variables, sized to the A12 free-tier ceiling), while the two comparators quoted alongside it in earlier drafts -- the frozen single-family pool at 0.7681 and the untuned mixed pool at 0.7565 -- are both k=13. Reporting a raw difference across different feature counts is the order-mismatched comparison ADR-0013 identifies as the mechanism by which apparent wins are manufactured, so it is not reported that way. The like-for-like comparator is the frozen single-family dct pool rebuilt AT k=6 on the identical splits, giving a PAIRED per-seed difference where only the pool composition varies. CORRECTED 2026-09-06 (see A15): an earlier version of this amendment stated the single-family pool scores 0.7688 at k=6 against 0.7681 at k=13 and concluded the tuned advantage was not a feature-count artifact. The 0.7688 was the mean of an exploratory FIVE-seed run and was carried into the ten-seed record without recomputation. The ten-seed mean is 0.7629, which is 0.0052 BELOW the k=13 figure, so six features are mildly worse for the frozen pool and part of any raw cross-k gap IS feature count. The conclusion drawn was therefore both unsupported and backwards. What stands unchanged: the +0.0198 reported in the paper is the PAIRED per-seed difference computed at matched k=6, so it is unaffected by the cross-k question entirely. Also recorded: a mid-run note predicted the tuned pool was unlikely to clear 0.7681 on the basis of sweep VALIDATION AP, which is not comparable to the TEST AP of the comparators; validation runs about 0.005 below test on this split design and seed 42's validation fold is markedly harder than its test fold. The prediction was wrong for that reason; the sweep's SELECTION was unaffected, since it ranked candidates against one another on a single consistent quantity.
- 2026-09-06 (A15): Fabricated comparator figure corrected (correction amendment; one reported number changed, no protocol content changed). The figure 0.7688, quoted in the proposal, appendix, Sprint 7 summary and amendment A14 as the frozen single-family pool's AUPRC at k=6, does not appear in any results artifact. It is the mean of an exploratory FIVE-seed run (seeds 42-46, mean 0.7687) that was carried into the TEN-seed writeup without recomputation. The correct ten-seed mean, recorded in `tuned_vs_frozen_k6.json`, is 0.7629. CONSEQUENCE, stated plainly because it inverts the argument the number was carrying: 0.7688 supported a claim that k=6 and k=13 perform identically for the frozen pool and therefore that the tuned arm's advantage is not a feature-count artifact. At the true 0.7629 the frozen pool is 0.0052 WORSE at k=6, so a feature-count effect exists and runs against the frozen arm; any raw comparison of the tuned k=6 figure to the k=13 frozen figure is inflated by it. UNAFFECTED: the +0.0198 the paper reports is the paired per-seed difference at matched k=6 (9 of 10 seeds positive, SD 0.0203, below the 0.0268 MDE), which never used the cross-k comparison. Found by an independent code review of PR #41, not by any test -- the third consecutive sprint in which the defect that travelled furthest was a claim about what a number MEANS rather than an error in a computation, which is what backlog item F35 exists to address.
- 2026-09-06 (A16): A13's intervention description narrowed to what the selected arm actually varies (correction amendment; no protocol content changed, no figure changed). A13 registered the intervention as "`class_weight='balanced'` passed to each weak learner as it is built", and the proposal, appendix, QCi letter and Sprint 7 summary each repeated "inside each weak learner". That is true of two families and not four: `dct` and `lg` accept `class_weight`, while `lda` and `knn` do not and received none. The selected configuration `balanced_shallow_knn` ALSO changes the KNN neighbourhood (3 neighbours, distance-weighted, against 5 uniform in the `balanced` variant), so the arm that shipped varies TWO things and the accuracy figure carries both. Attribution recorded separately because the two figures behave differently: the DIVERSITY result is attributable to class weighting alone, since `balanced` (KNN at defaults) reaches gram ratio 0.9220 and the selected arm 0.9287, both far below the 0.9988 of the untouched pool; the ACCURACY result is not cleanly attributable, since `balanced` reaches 0.7019 validation AP and the selected arm 0.7133, so roughly half the selected candidate's edge over `balanced` comes from the neighbourhood change rather than from class weighting. The +0.0198 matched test-set difference stands as measured -- it compares the selected pool against the frozen pool and does not depend on which of the two interventions produced it -- but the CAUSAL sentence attributing it to class weighting alone was unsupported and has been corrected in all four documents. Found by an independent code review of PR #41.
- 2026-09-06 (A17): PROTOCOL VIOLATION found and corrected in the A11/A13 exploratory arms (protocol-compliance amendment; every A11 and A13 figure recomputed). Section 4 of this preregistration removes 1,081 exact duplicates BEFORE splitting, and every arm's loader implements it (`qubo_proxy`, `run_classical`, `run_hardware`, `pilot_variance` all call `.drop_duplicates()`). The fold builders written for the exploratory pool work -- `mixed_pool.run_seed`, `tuned_pool._fold` and `run_hardware_f32.run_seed` -- did NOT: they called `data.load_ulb()` directly and trained on 284,807 rows against every comparator's 283,726. The violation was invisible for two sprints because those arms were only ever compared against each other. It surfaced when a generator was written for the matched comparator (PR #41 review finding 5) and its regeneration disagreed with the committed figures on all ten seeds, with mixed signs -- the signature of a different dataset rather than a different parameter. HOW IT REACHED THE PAPER: the F31 and F33 arms are the newest code in the project and were built as standalone modules rather than through the shared loader, so they inherited none of its protocol enforcement. CONSEQUENCE: every A11 (F31 mixed pool) and A13 (F33 tuned pool) figure was computed on a superset of the preregistered dataset, including the 1,081 duplicate rows that inflate any metric by letting identical transactions appear in both train and test. All three fold builders now deduplicate, and every affected figure is recomputed and republished; the frozen arms (H1b, G0, G0b, the hardware campaign) are UNAFFECTED, since their loaders were always correct. This is recorded as a violation rather than a refinement because that is what it was. EFFECT ON THE REPORTED RESULT, stated in full because the correction moves the F33 finding ACROSS the significance threshold in our own favour, which warrants more scrutiny rather than less: on the corrected data the tuned pool scores 0.7700 against 0.7827 before, the matched frozen comparator scores 0.7381 against 0.7629 before, and the paired difference rises from +0.0198 (9 of 10 seeds, BELOW the 0.0268 MDE) to +0.0319 (10 of 10 seeds, EXCEEDING it). The violation was MASKING the effect, not creating it. The mechanism is measured rather than assumed: the 1,854 rows in duplicate groups carry a 1.73% fraud rate against the dataset's 0.17%, a TENFOLD enrichment, so deduplication removes disproportionately many memorisable positives. The frozen arm is a pool of depth-limited decision trees, which split a duplicated row exactly, while the tuned pool's LDA, logistic and k-nearest-neighbour families average over neighbourhoods and memorise less; the arm that gained most from the duplicates therefore lost most when they were removed (-0.0248 against -0.0127). We report the corrected figure and this explanation together, and we note plainly that a result which improves after a self-reported protocol violation deserves a reviewer's scepticism -- the generator, the data and the per-seed values are all in the repository for that purpose.
- 2026-09-07 (A18): H6 representation arm registered and run (exploratory-arm amendment; no protocol content changed, no existing figure changed). Section 3's H6 asks whether the QFE phase representation SHIFTS the quantum-minus-classical delta, and requires every cell's classical bar to carry a trained-frequency GAM, a GA2M and an order-matched JOINT twin alongside the GBDTs. This amendment records the operational choices made to run it, none of which were selected after seeing a result. IMPLEMENTATION: `run_h6.py`; ULB stratified protocol, ten seeds 42-51, k=13 sequential pool at schedule 2 matching the frozen CVQBoost configuration; the CVQBoost arm is the exact classical proxy of the identical Hamiltonian [SIM], zero metered seconds. TWINS: GAM is statsmodels GLMGam with BSplines smooth terms on the six highest-variance columns; GA2M is sklearn HistGradientBoostingClassifier with interaction_cst='pairwise', which is the GA2M contract of main effects plus pairwise terms only; JOINT is a supervised coarse-to-fine cosine frequency scan over the eight highest-variance columns, refined around the best coarse frequency and fit by logistic regression. All three primitives were pre-flighted in Sprint 6 (`h6_twin_preflight.py`); pygam and interpret are not installed and are not used. REPORTED QUANTITY: the SHIFT, mean(delta_QFE) - mean(delta_baseline), where delta is CVQBoost minus the BEST classical arm in that cell, the twins included in that maximum. A delta under a single representation does not answer H6 and is not reported as if it did. FALSIFIER, fixed before the run: the premise that the representation could move the delta is disproved when the absolute shift falls below the paired seed-to-seed standard deviation of the shift itself, and that outcome is published as the H6 answer rather than as a failed run. SCOPE: exploratory, carries no confirmatory claim; H1b remains the sole confirmatory endpoint. The QFE encoder is fitted on TRAIN folds only, asserted by test.

## 1. Central claims under test

Working theory: the Dirac-3 CVQBoost pipeline delivers (a) detection quality competitive with tuned gradient-boosted baselines, with measured wins inside structured sub-segments, (b) training-time scaling advantages as rows and features grow, and (c) representation-conditioned behavior that differs from classical models under quantum-native (phase) feature engineering. The submission's goal is best achievable detection plus an honest map of where the quantum-inspired component earns its place.

**Compound falsification criterion.** The central theory is considered unsupported if ALL of the following hold: H1b fails on the primary endpoint, the H3 dose-response slope is not positive, and H5 does not transfer to any fraud segment. In that case the submission's headline becomes the measured regime map and boundary statement, a pre-committed fallback, not a post-hoc pivot.

**Provenance disclosure.** H5 and the starting quantum configuration derive from a prior exploratory sweep (FourierWall2 campaign, 2026-08-04) on SPECTRA data that block B4 partially reuses. They are exploratory-derived hypotheses given a confirmatory test here, not blind predictions. Any configuration re-tuning for fraud segments uses its own held-out split and never touches transfer-test data.

## 2. Datasets

| Role | Dataset | Notes |
|---|---|---|
| Primary endpoint + calibration | ULB European Cardholder (`XGBvHQXGB\datasets\creditcard.csv`, 284,807 rows, 492 frauds, 0.172%) | Corroborated clean-protocol reference band: AUPRC 0.85-0.88, AUC-ROC 0.97-0.98 (see research report; results above ~0.95 AUPRC in the literature are leakage artifacts). Spans 2 days, so temporal splitting is a stated-weak sensitivity check. |
| Scaling and regime headline (exploratory) | IEEE-CIS (Kaggle, 590,540 train rows, 3.5% fraud) | Competition published AUC-ROC only (winner 0.9459 private); leakage-free reproduction band AUPRC 0.64-0.67 is the honest reference and is attributed to the reproduction, never to the competition. |
| Pipeline-correctness check only | Sparkov (Kaggle shipped fraudTrain/fraudTest temporal split) | Saturated at AUC-ROC 0.995-0.998; deterministic generator. Declared in advance: no substantive fraud-performance conclusions rest on it. |
| External-validity replication (NOT fraud evidence) | SPECTRA quantum-ready datasets (4 Kaggle datasets, CC BY 4.0), published at https://www.kaggle.com/javierfalcondale/datasets: spectra-energy-quantum-ready-steel-plant-data, spectra-oil-and-gas-quantum-ready-gas-turbine-data, spectra-maintenance-quantum-ready-failure-data, spectra-telecom-quantum-ready-churn-data | Used only for the H5(i) replication of the prior in-segment result; results reported as replication of a structural finding, never as fraud evidence. Cite both the releases and their original UCI substrates (851, 551, 601, 563) per docs/references.md. |

AUPRC baselines equal prevalence (ULB 0.0017, IEEE-CIS 0.035, Sparkov ~0.057); prevalence is reported beside every AUPRC value, and AUPRC is never compared or averaged across datasets.

## 3. Hypotheses and gates

**Confirmatory structure.** There is exactly ONE primary confirmatory endpoint (H1b-primary). All other hypotheses and cells are exploratory: reported with Holm-adjusted intervals across the K exploratory cells actually run, K fixed at freeze time in section 10, and labeled exploratory in the proposal.

- **G0 (pipeline honesty gate):** mean AUPRC of tuned XGBoost across the 10 stratified ULB seeds (full features, weighting-only) reaches >= 0.85, the bottom of the corroborated clean band. Below that, the pipeline is presumed defective and everything halts. TrueLoop's 0.8937 is a reference under an unverified protocol, not the gate. **G0c:** ULB preprocessing is fixed in advance: `Time` retained as a raw feature and as a time-of-day phase (QFE arm), `Amount` log-transformed, no row dropped except exact duplicates (counts reported).
- **G0b (proxy fidelity gate):** the top-3 and bottom-2 proxy-ranked CVQBoost configurations each run once on Dirac-3 (5 budgeted fits). If the Spearman rank correlation between proxy and hardware AUPRC is < 0.5, proxy-selected configurations are declared non-transferable, and all hardware results are reported as single-config points with no tuning claim.
- **H1a (reproduction, exploratory):** the SMU/OCBC CVQBoost configuration (heterogeneous KNN/LDA/LG/XGB weak-classifier pool) reproduces AUC-PR >= 0.80 on ULB under the SOURCE paper's protocol (70/30 split, train-only SMOTE, 10 seeds), run and reported as a labeled reproduction, separate from all house-protocol tables.
- **H1b-primary (THE confirmatory endpoint):** on ULB, stratified protocol, matched top-k feature set, the best preregistered CVQBoost configuration beats the best of the tuned GBDT trio on AUPRC. Decision statistic: per-seed paired delta-AP on identical test rows; decision rule: the t-interval on the across-seed mean (10 seeds) excludes zero; per-seed paired stratified BCa CIs (2,000 resamples) reported as support. Executed on Dirac-3 hardware if access permits; otherwise evaluated on the proxy, labeled [SIM], with the [HW] confirmation deferred to Phase 2 under this committed grid. The gate's status is never ambiguous.
- **H1b-exploratory:** the same comparison on IEEE-CIS and on the temporal protocols, reported with Holm adjustment.
- **H1c (training economics, exploratory):** fit log(training time) = a + b log(n_rows) over the row ladder {25k, 50k, 100k, 200k, and 400k on IEEE-CIS} at fixed feature count, per arm. Passes if CVQBoost's slope b is below every GBDT's slope with non-overlapping 95% CIs. Hardware disclosed (CPU model, cores, GPU if any); GBDTs timed at 1-core and all-core; CVQBoost wall-clock INCLUDES weak-learner construction and Hamiltonian build, with the Dirac solve time also broken out.
- **H2 (expected null, exploratory):** on ULB full-feature unmatched comparisons, the GBDT ceiling stands. A CVQBoost win here would be reported but treated as surprising.
- **H3 (regime dose-response, exploratory):** on ULB and IEEE-CIS separately, run the frozen CVQBoost config and best GBDT at k in {5, 9, 13, 17} matched features; report the slope of delta-AUPRC vs k with a seed-clustered CI. Scored only if at least 3 ladder cells per dataset run; otherwise reported descriptively.
- **H4 (structural attribution, exploratory):** CVQBoost minus the BEST structural control, per cell. Controls: the identical weak-learner output matrix with weights fit by (i) non-negative ridge with lambda tuned over the same grid as CVQBoost's lambda_coef, and (ii) a non-negative sparse (L1) variant. Only this delta may be attributed to the Dirac solve.
- **H5 (in-segment advantage, exploratory-derived):** (i) replicate the SPECTRA in-segment result with 5 seeds on the 3 strongest cells, reporting mean, std, CIs; (ii) transfer test on fraud: segments defined a priori on train-only statistics by frozen predicates computable without labels at inference (Sparkov fraud-category pockets; IEEE-CIS device/email cohorts), evaluated in-segment vs overall for every arm, with a matched random-segment control (same size and base rate; the reported edge is in-segment minus random-segment) and a minimum of 50 test positives per scoreable cell; (iii) track the in-segment train-test generalization gap and test whether stronger regularization closes it without losing the win.
- **H6 (representation effect, exploratory):** the QFE phase representation (exact Fourier Wall recipe: rank phases, log magnitudes first, calendar cycles, train-only whitening, low-cardinality phases excluded from encoded blocks) is given to EVERY arm. Question: does it shift the quantum-minus-classical delta? Every H6 cell's classical bar includes trained-frequency GAM, GA2M, and an order-matched JOINT twin (supervised k-way cosine search fit by logistic regression) in addition to the GBDTs, per the Fourier Wall's demonstration that omitting the twin manufactures fake quantum wins.

Null results are published as nulls. All gates are scored as committed in the scoring table (section 11).

## 4. Model arms

| Arm | Specification |
|---|---|
| XGBoost, LightGBM, CatBoost | Tuned per section 6. NaN passed through natively (no sentinel imputation); library categorical handling used explicitly; CatBoost `boosting_type` set explicitly (Ordered claimed only if set); early stopping on a dedicated validation fold, never test; stopping iteration, patience, and monitored metric reported. |
| Logistic regression | Floor control, matched features. |
| CVQBoost (QBoostClassifier, Dirac-3) | Heterogeneous weak pool (KNN, LDA, LG, shallow XGB) and the FourierWall2 tuned settings as the starting configuration: schedule 3 where variables fit, num_samples=8, relaxation_schedule=2, lambda_coef = 2 x n_train, default weak learners, `weak_cls_strategy="sequential"` on Windows. All tuning on the proxy. |
| Structural controls / proxy | The identical QUBO objective solved classically: non-negative ridge (and L1 variant) over the same weak-learner outputs. Serves as both the tuning proxy and the H4 attribution control. |
| Trained-frequency GAM, GA2M, JOINT twin | H6 cells only; JOINT = few k-way cosine terms found by supervised coarse-to-fine frequency scan, fit by logistic regression. |
| Gate-based arm (Braket simulator, [SIM]) | PHASE-ACTIVE encoding only (sandwich or entangling encoding); plain Ry angle encoding is banned as provably classical (Inverse Born Rule). The arm's phase complexity C, Berry-connection magnitude, and mode mutual information are computed and reported as evidence the quantum framing is non-superficial. |
| QSVM (sign-augmented primal, Dirac-3) | Cheap secondary arm, ~1 metered second per fit. |

## 5. Feature handling and leakage controls

1. Every transform (scaling, encoding, imputation, MI selection, whitening, aggregation) is fit on training data only, inside a fitted pipeline, per fold.
2. Feature selection runs inside the CV loop (Cawley-Talbot).
3. **IEEE-CIS reduced recipe, preregistered:** D-column time normalization (Dn = D - TransactionDT/86400 for D1, D2, D4, D10, D11, D15); one UID = card1 + addr1 + floor(day - D1), the UID itself EXCLUDED from the model; UID aggregations limited to this named list: TransactionAmt mean and std, C1-C14 means, D4/D9/D10/D15 aggregates; frequency encodings of card1_addr1 and card1_addr1_P_emaildomain; V-column reduction by missing-pattern grouping and within-group correlation (>0.75, keep highest cardinality). No client-mean post-processing; no model blending.
4. Time-consistency filter (single-feature train-early/test-late AUC must exceed 0.5) and adversarial validation (target AUC near 0.5, recursively dropping top adversarial features) both run inside training folds only.
5. Temporal protocols: aggregates use only rows strictly before the split boundary; no identifier column enters any model raw.
6. Positive control: shuffled-label run must collapse test AUPRC to the base rate; reported.
7. Exact duplicates removed before splitting; counts reported per dataset.

## 6. Tuning protocol and budget equivalence

- Equal budget: 100 Optuna TPE trials per arm per dataset (literature anchors: Grinsztajn ~400 random-search iterations; Shwartz-Ziv 1,000; McElfresh 30; the "60 trials" folklore is verified absent from Bergstra-Bengio and is not cited). Logistic: 25 trials.
- GBDT search spaces as in v1.0 (unchanged); optimization target average precision; early-stopping on `aucpr`/`average_precision`/`PRAUC`, headline numbers always from `average_precision_score`.
- CVQBoost tunes on the PROXY only (section 4), equal trial budget, over weak pool composition, schedule, k, lambda alpha in {0.5, 1, 2, 4}; num_samples and relaxation_schedule are FIXED from prior hardware evidence and not tuned.
- A Tuning Budget Equivalence table is published: trials, model fits per trial, and wall-clock CPU-hours per arm, reported as-is with the asymmetry acknowledged.

## 7. Class imbalance

Weighting only in house protocol (`scale_pos_weight` / `auto_class_weights`; exactly one mechanism per library). No resampling on top of weighting, ever; no resampling before splitting, ever. H1a's SMOTE (train-only, inside folds) is an isolated labeled reproduction. Because weighting degrades probability calibration, an uncorrected variant of the best GBDT is reported alongside for the calibration section.

## 8. Evaluation protocols and splits

1. **Stratified (primary):** ULB 60/20/20, stratified, seeds {42..51} for the primary cell (10 seeds; splits vary with seed per Bouthillier), {42..46} for exploratory cells.
2. **Temporal:** IEEE-CIS: GroupKFold by calendar month with 3-fold rolling-origin evaluation. Sparkov: the shipped fraudTrain/fraudTest split. ULB: single 70/10/20 time-ordered split, labeled a sensitivity check (2-day span; stated, not overclaimed).
3. A minimum-detectable-effect statement is computed from pilot seed variance before hardware runs; observed margins below the MDE are reported as indistinguishable, not as wins.

## 9. Metrics and statistics

- AUPRC primary, computed as step-wise `average_precision_score` (never trapezoidal); tie-frequency check reported, cross-checked against a second implementation if ties are common. AUC-ROC secondary. Prevalence beside every AUPRC.
- Thresholded metrics (F1, precision, recall, confusion matrix) at two validation-chosen operating points: max-F1, and a fixed alert budget (card precision at k). Wilson intervals for precision and recall; bootstrap for F1. Validation-to-test threshold drift reported as a robustness metric under temporal protocols.
- CIs: stratified BCa bootstrap, 2,000 resamples. Paired comparisons: identical resample indices on both arms' predictions.
- Within-dataset model comparisons: paired tests on identical folds and seeds (corrected resampled t-test or 5x2cv where refits allow; DeLong for AUC-ROC pairs; paired bootstrap for AUPRC). No cross-dataset omnibus test at N=3; no cross-dataset averaging of any metric. Holm correction across the K exploratory cells; effect sizes reported with every interval.
- Calibration: fit on a held-out fold via CalibratedClassifierCV; Platt scaling where positives are scarce (ULB), isotonic where supported (IEEE-CIS); reliability diagrams plus ECE with equal-mass binning and a stated bin count; Brier reported only as a joint score, never as a calibration measure.
- Every quantitative claim carries an evidence tag: [HW], [SIM], or [PROJ].

## 10. Phase 1 hardware run grid v2 (the only metered runs; each block requires team-lead approval)

Variable budget (corrected by A2; side-by-side build option added by A3): sequential build vars = C(n,2) at schedule 2, C(n,2) + C(n,3) at schedule 3; full-pair build adds n. Free tier ~100 vars (n <= 13 at schedule 2, 78 sequential / 91 full-pair); documented device ceiling 949 (n <= 17 at schedule 3, 816 sequential / 833 full-pair).

| Block | Runs | Est. QPU s | Gated on |
|---|---|---|---|
| B1: ULB free-tier configs (top-13, schedule 2, 78 vars), 2 pool variants x (10-seed primary + temporal sensitivity) | 22 fits | ~300-600 | current ~500 s balance |
| G0b: proxy-fidelity fits (top-3 + bottom-2 configs) | 5 fits | ~100 | current balance or grant |
| B2: ULB full config (top-17, schedule 3, 833 vars), 10-seed primary + temporal | 11 fits | ~450 | QCi grant |
| B3: IEEE-CIS reduced-recipe subset, full config, 5 seeds x 2 protocols + H3 ladder cells (k in {5,9,13,17}, 5 seeds at k=17 already counted; 3 extra k x 2 seeds) | 16 fits | ~650 | QCi grant |
| B4: SPECTRA replication, 3 strongest in-segment cells x 5 seeds | 15 fits | ~450 | QCi grant + SPECTRA re-download |
| B5: QSVM arms | 12 fits | ~15 | current balance |
| Contingency (~15%; errored solves retried at most twice, retries counted) | | ~350 | |
| **Total** | **81 fits** | **~2,300-2,600** | |

Granted-seconds spend priority: B3 > B2 > H3 ladder > B4 > contingency. K (the number of exploratory cells for Holm) is fixed at freeze as the count of scoreable cells in this grid. If no grant arrives by Sep 8: B1, G0b (partial), B5 run on the current balance; H1b-primary is evaluated on the proxy as [SIM]; B2-B4 enter the proposal as [PROJ] with this grid cited as the committed plan; the proposal is submitted regardless.

## 11. Scoring, amendments, and deviations

- **Gate table:** every gate/hypothesis is published in a table with columns Gate, Prediction, Outcome (pass / fail / null / unscoreable), Evidence tag, Amendment ref. Nulls included.
- **Amendments** may add exploratory analyses or fix implementation bugs, with date and reason. They may NEVER change a gate's pass/fail criterion, add cells to an existing hypothesis after its data is observed, or remove an observed hypothesis; any such change is a reported DEVIATION.
- **Analysis code freeze:** metric and analysis code freezes at the same commit as this file; later changes are dated amendments.
- **Failed runs:** an errored hardware solve is retried at most twice with identical config and seed; retry counts reported per cell; three failures = the cell is reported as failed, never silently dropped.
- **results.json schema (required keys):** arm, dataset, protocol, seed, config_hash, features_used, metrics (all of section 9), evidence_tag, metered_seconds, retry_count, timestamps.

## 12. Reproducibility and freeze procedure

Pinned environment (library versions shift GBDT results measurably), fixed seeds, all configs and results in results.json, public repository at Stage 7 after the confidentiality scan.

Freeze: (1) the team lead reviews and approves; (2) `git init` if needed, commit this file plus `experiments/src/`, record the commit hash here by amendment; (3) amendments only thereafter, per section 11.
