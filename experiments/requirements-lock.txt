# EXACT resolved versions for every figure in this submission (F54).
#
# requirements.txt states ranges, which is right for development and wrong for
# a reproducibility claim: `pip install -r requirements.txt` today resolves a
# DIFFERENT environment than the one that produced these results, and the drift
# is already material -- xgboost 2.1 -> 3.4 is a major version, scikit-learn
# 1.5 -> 1.9 is four minors, and both sit in the classical comparator stack the
# null result is measured against.
#
# Captured from the working interpreter via importlib.metadata on 2026-09-10.
# The venv ships no pip module, so `pip freeze` is unavailable; this file is the
# export. Use it, not requirements.txt, to reproduce a reported figure.
python==3.12.10

numpy==1.26.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.9.0

xgboost==3.4.1
lightgbm==4.7.0
catboost==1.2.10
imbalanced-learn==0.14.2

eqc-models==0.21.0
qci-client==5.0.2

optuna==4.9.0
shap==0.49.1
statsmodels==0.15.0
joblib==1.5.3
matplotlib==3.11.1
python-dotenv==1.2.3
pypdf==6.17.0
pytest==9.1.1
